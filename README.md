<p align="center">
  <img src="app/static/img/makerhub-logo.png" width="160" alt="makerhub logo">
</p>

# makerhub

> 此项目由 Codex 生成，并在此基础上继续迭代。
> makerhub 基于 [mw_archive_py](https://github.com/sonicmingit/mw_archive_py) 的抓取思路二次重构而来，感谢原作者 [sonicmingit](https://github.com/sonicmingit) 的开源分享。

一个从零重建的 MakerWorld 本地归档后台。

## 项目介绍

makerhub 是一个面向个人 NAS / DSM 部署的 MakerWorld 模型归档与管理系统。它通过用户配置的 Cookie 和代理能力抓取 MakerWorld 模型信息、图片、评论、打印配置和 `3MF` 文件，并把这些内容长期保存到本地目录中，方便在内网或公网统一浏览、搜索、补档和维护自己的模型库。

它的定位不是公开模型站，而是“个人 MakerWorld 本地资料库”。你可以归档单个模型，也可以批量归档作者页、收藏夹、合集页；可以为这些来源创建订阅并定时同步；也可以把本地已有的 `3MF` 文件导入并自动整理入库。现在设置页还内置了系统更新中心，可直接查看当前版本、线上版本、最近更新日志，并在 Docker Compose 部署下触发网页一键更新。

## 功能概览

- 首页展示台：集中展示模型总数、缺失 `3MF`、归档任务、订阅、源端刷新和本地整理等关键状态。
- 模型库：以卡片方式浏览已归档模型，支持搜索、来源筛选、标签筛选、收藏、已打印、源端删除和本地删除等状态过滤。
- 模型详情页：查看单个模型的主图、图集、打印配置、附件文档、评论回复树、打印实例和 `3MF` 下载状态。
- 单模型归档：输入单个 MakerWorld 模型链接，归档模型基础信息、图片、评论、打印配置和 `3MF` 文件。
- 作者页批量归档：输入作者上传页链接，批量扫描并归档该作者尚未入库的模型。
- 收藏夹 / 合集批量归档：支持批量扫描收藏夹、合集详情页和模型集合链接，并加入归档队列。
- 订阅：对作者页、收藏夹、合集建立定时同步任务，发现新模型后自动加入归档队列。
- 源端刷新：定期刷新已归档模型的评论、附件、打印配置和源端删除状态，已成功下载的 `3MF` 不会重复下载。
- 缺失 `3MF` 重试：识别已归档但 `3MF` 未成功获取的打印配置，并支持重试与失败原因查看。
- 本地 `3MF` 整理：监控本地目录，把手动放入的 `3MF` 自动解析、去重、整理并导入模型库。
- 重复检测与配置合并：本地导入时会尽量识别是否与现有模型重复，并把同模型的不同打印配置合并到同一条目下。
- 软删除与源端删除标记：本地删除采用软删除机制；源端删除只做标记，不会自动删除本地归档内容。
- 连接设置：提供国内 Cookie、国际 Cookie 和 HTTP 代理配置，适配不同 MakerWorld 访问环境。
- 用户与安全：支持单用户登录、Token 管理和公网部署场景下的基础访问控制。
- 系统更新中心：在设置页集中查看当前版本、GitHub 最新版本、最近更新日志和更新状态；Compose 部署挂载 `docker.sock` 后，可直接拉取最新镜像并重建当前容器。
- 日志：集中查看归档、订阅、源端刷新、本地整理、缺失 `3MF` 和系统错误等业务日志。
- Docker / DSM 部署：适合通过 Docker Compose 或 Synology DSM Container Manager 部署，配置、日志、状态和归档目录分离挂载；仓库内的 `compose.yaml` 已默认打开网页更新所需的 `docker.sock` 挂载。

## 更新记录

### 2026-04-27
- 版本号升级到 `v0.5.106`
- 设置页现有库信息补全增加运行态显示和自动轮询：后台扫描启动后按钮显示“扫描中”，页面每 3 秒刷新状态，完成后直接显示发现缺失、新增入队、已在队列和失败数量
- 补全状态文案不再依赖手动刷新，避免后台任务完成后页面仍停留在 0 / 等待执行

### 2026-04-27
- 版本号升级到 `v0.5.105`
- 现有库信息补全改为后台启动：按钮点击后立即返回，不再等待完整扫描和 900+ 模型入队完成，避免反代超时页被前端识别成“接口返回 HTML 页面”
- 补全状态接口会截断过长的入队明细，仅保留计数和前 50 条样例，减少设置页刷新状态时的大响应和卡顿

### 2026-04-27
- 版本号升级到 `v0.5.104`
- 评论采集新增 MakerWorld `commentandrating` 主列表分页，不再只依赖模型页面首屏数据；归档、订阅、源端刷新、缺失 `3MF` 重试和现有库信息补全都会复用这条链路
- 评分评论的 `instRatingReply` 会按评论回复树保存，回复不足时改走 `rating/{id}/reply` 补全；普通评论回复继续走 `comment/{id}/reply`
- 现有库信息补全会把 `commentCount` 大于本地评论树数量的模型重新列入候选，方便把过去只抓到第一页评论的模型补齐

### 2026-04-26
- 版本号升级到 `v0.5.103`
- 源端刷新页面抓取的 `curl` 兜底增加 TLS 兼容重试：默认请求失败后会依次尝试 `HTTP/1.1 + TLS1.2` 和 `IPv4 + HTTP/1.1 + TLS1.2`，减少 `curl code=35 unexpected eof while reading` 导致整轮刷新失败的问题

### 2026-04-26
- 版本号升级到 `v0.5.102`
- 修复手动重试缺失 `3MF` 被旧每日上限状态短路的问题：手动单条/批量重试会先清除本地旧暂停标记并重新访问 MakerWorld 检测；如果源端本次仍返回每日上限，再重新写入首页和缺失列表状态
- 自动重试、源端刷新和新增 `3MF` 下载仍会尊重新的每日上限保护，避免真实触发上限后继续反复排队

### 2026-04-26
- 版本号升级到 `v0.5.101`
- 设置页连接设置新增国区 / 国际区每日 `3MF` 下载上限，默认各 `100`，可手动调整
- 归档、缺失 `3MF` 重试和新增 `3MF` 下载在获取下载地址前会按站点扣减每日额度；达到 MakerHub 本地上限后会暂停到次日 00:00，并复用现有每日上限提示链路

### 2026-04-26
- 版本号升级到 `v0.5.100`
- 修复首页和任务页待补 `3MF` 摘要继续显示旧暂停日期的问题：`download_limited` 项会按当前 `three_mf_limit_guard` 重新生成“自动重试暂停至”时间，避免昨天的 `00:00` 被缓存到今天

### 2026-04-26
- 版本号升级到 `v0.5.99`
- 新增独立的轻量 UI 请求执行池，首页启动、配置、任务状态、源端刷新状态、归档事件流和后台修复状态不再与模型列表/详情等重型 Web 读取共用同一个线程池
- 手动源端同步入口改走任务 API 执行池，避免触发任务的动作占用 UI 状态读取通道

### 2026-04-26
- 版本号升级到 `v0.5.98`
- 处理依赖安全告警：升级 `FastAPI / Starlette` 修复静态文件 `Range` 请求 CPU 消耗风险，升级 `requests`，移除未使用的 `uvicorn` standard extras，并把前端 `postcss` 更新到无告警版本
- 附件上传入口新增请求体大小上限，默认 `128MB`，可通过 `MAKERHUB_MAX_MANUAL_ATTACHMENT_BYTES` 调整；附件落盘时也会按同一上限中断并清理临时文件

### 2026-04-26
- 版本号升级到 `v0.5.97`
- 修复首页源端状态卡和缺失 `3MF` 重试提示不一致的问题：每日上限提示统一读取当前限流 guard 的暂停时间，不再复用缺失列表里缓存的旧日期
- 缺失 `3MF` 列表里的旧 `download_limited` 记录只有在当前站点仍处于有效每日上限保护时才会覆盖首页状态，避免首页卡片被昨天的失败消息卡住

### 2026-04-25
- 版本号升级到 `v0.5.96`
- 修复 MakerWorld 评论回复补抓：回复接口返回的顶层 `replies` 现在会直接挂回对应主评论，不再只留下“共 8 回复”这种不可展开计数
- 兼容 MakerWorld 评论列表里的 `commentReply` 字段，列表内自带的前几条回复会保存到本地 `comments[].replies`
- 源端刷新时如果新抓取结果没有带评论列表，也会用本地已有评论继续补全缺失回复，避免旧库只保留回复数
- 现有库信息补全不再只看评论 schema 版本，只要发现 `replyCount` 大于本地 `replies` 数量，就会重新入队补回复
- 评论区展开状态改用每条评论加载时生成的唯一线程 key，避免点击一个“查看更多回复”时把其他评论一起展开或收起

### 2026-04-25
- 版本号升级到 `v0.5.95`
- 修复评论抓取误判问题：避免把 MakerWorld 页面里的“模型描述 / 评论 / 描述 / Model Description”等界面文案识别成匿名评论
- 详情页读取旧归档数据时会隐藏这类历史假评论，真实评论和评论回复继续保留显示

### 2026-04-25
- 版本号升级到 `v0.5.94`
- 缺失 `3MF` 发起重试后会立即清除首页源端状态卡上的旧验证/上限失败覆盖；如果重试过程中再次遇到验证或每日下载上限，再把首页状态更新为对应失败

### 2026-04-24
- 版本号升级到 `v0.5.93`
- 首页源端状态卡和任务页缺失 `3MF` 列表在读取旧状态文件时，也会把历史验证失败文案统一转换为“MakerWorld 需要验证，前往官网任意下载一个模型”

### 2026-04-24
- 版本号升级到 `v0.5.92`
- MakerWorld 验证提示文案统一改为“MakerWorld 需要验证，前往官网任意下载一个模型”，让首页源端状态卡和缺失 `3MF` 失败原因更直接

### 2026-04-24
- 版本号升级到 `v0.5.91`
- 缺失 `3MF` 状态现在会从错误信息里统一识别“需要验证 / Cloudflare / Cookie 失效 / 每日下载上限 / 源端无文件”等具体原因，不再全部混成 `missing`
- 归档队列、任务状态和源端健康卡共用同一套 `3MF` 失败状态归一化逻辑，避免源端明明触发验证，任务页和首页仍显示普通缺失
- 任务页缺失 `3MF` 列表补充更明确的状态文案，方便直接区分“需要验证”“到达每日上限”和“源端无文件”

### 2026-04-24
- 版本号升级到 `v0.5.90`
- 源端刷新页把原来的“刷新状态”按钮改成了真正的“手动同步”按钮，点击后会立即触发一轮源端刷新，而不是只重新读取状态
- 页面状态继续保留自动轮询，手动同步运行中按钮会自动禁用，避免重复触发多轮源端刷新
- 后端新增手动触发入口，支持在不改 Cron 的情况下临时跑一轮源端同步；即使计划任务被关闭，只要当前服务空闲也能单独手动执行

### 2026-04-24
- 版本号升级到 `v0.5.89`
- 评论归档主链路改为在检测到顶层评论只有 `replyCount`、但本地还缺少 `replies` 时，额外请求 MakerWorld 的 `comment-service /comment/{id}/reply` 接口，把回复真实挂回对应主评论后再落盘
- 这条回复补抓链路会同时覆盖单模型归档、订阅入队后的归档、源端刷新、缺失 `3MF` 重试和“现有库信息补全”，不再只修前端展示兜底
- 评论 schema 再升一版，旧库更新到此版本后可直接通过“设置 -> 补全现有库信息”重新扫描并补回此前缺失的评论回复

### 2026-04-24
- 版本号升级到 `v0.5.88`
- 评论区前端新增顶层平铺回复兜底重挂逻辑：即使接口返回里仍有回复混在顶层数组中，详情页也会先在浏览器侧挂回对应主评论，再按默认 3 条回复展示
- 对“有回复数但当前还没挂到主评论下”的条目，不再显示不可点击的静态文案，而是提供“继续加载回复”按钮，避免评论区看起来像完全失效

### 2026-04-24
- 版本号升级到 `v0.5.87`
- 评论归档主链路现在会在写入 `meta.comments` 前统一重建成“顶层评论 + replies 子树”结构，不再把回复与主评论长期混存同一层
- 源端刷新合并评论后也会再次按树结构落盘，避免刷新后把回复重新拍平成顶层项
- 缺失 `3MF` 重试、订阅新模型入队和“现有库信息补全”最终都复用同一条评论落盘逻辑；旧库补全也会额外识别顶层平铺回复并重新入队修复

### 2026-04-24
- 版本号升级到 `v0.5.86`
- 评论线程归并继续补强：当旧归档里的回复缺少可用 `rootCommentId`，但仍保留 `replyToName` / `beRepliedUser` / `commentType` 等回复信号时，会按原始顺序兜底挂回前一个待补回复的主评论
- 修复一批旧模型里“回复内容已抓到，但主评论下 `replies` 仍为空”的情况，更新后详情页评论区才能真正按默认 3 条回复预览并支持展开

### 2026-04-24
- 版本号升级到 `v0.5.85`
- 修复网页一键更新的容器切换保护：更新 helper 现在会在删除旧容器前轮询新容器是否持续存活，避免新镜像启动即退出时把站点直接更新没了
- 更新失败时会尽量带回新容器的启动日志，方便直接在更新状态和业务日志里判断是镜像启动失败、健康检查失败还是超时未恢复

### 2026-04-24
- 版本号升级到 `v0.5.84`
- 修复旧归档里“评论回复被拍平成顶层评论”时的详情页线程结构：现在会按 `rootCommentId` 重新挂回主评论，恢复和 MakerWorld 一致的回复树
- 模型详情页评论区重新拿到正确的 `replies` 列表后，包含回复的评论会默认直接展示前 3 条，剩余部分通过“查看更多回复”展开，不再只剩一个不可展开的回复计数

### 2026-04-23
- 版本号升级到 `v0.5.83`
- 现有库信息补全、归档整理和源端刷新现在会一起补抓评论回复，旧库里只有回复数但没有实际子回复内容的模型，也能重新扫描并回填回复树
- 模型详情页图片预览改成纯图片悬浮层；详情主图缩略图库和打印配置悬浮窗图库改为按实际溢出显示左右按钮，并去掉原生滚动条与悬浮缩略图高亮时的绿色竖线

### 2026-04-23
- 版本号升级到 `v0.5.82`
- 设置页默认入口改回“系统”标签，侧边栏“设置”和用户菜单里的“系统设置”不再直接跳到“连接设置”
- 首页国内站 / 国际站状态卡现在会合并缺失 `3MF` 里的下载阻塞状态；当 `3MF` 下载已触发站点验证、Cloudflare 或每日上限时，不再继续误显示为“连接正常”

### 2026-04-23
- 版本号升级到 `v0.5.81`
- 设置页标签顺序调整为“系统 / 连接设置 / 用户 / 通知”，与当前页面信息分组保持一致
- 模型详情页评论区改为默认先展示 3 条回复，超过部分通过“查看更多回复”展开，更接近源站阅读方式
- 评论归档与源端刷新现在会保留评论回复树，修复过去只显示回复数、但没有把子回复内容真正同步回本地的问题

### 2026-04-23
- 版本号升级到 `v0.5.80`
- 任务页和缺失 `3MF` 状态读取会过滤“信息补全任务仅整理打印配置详情，不下载 3MF”这类占位记录，不再把信息补全误显示成真实缺失
- 源端刷新重建缺失 `3MF` 列表时也会跳过这类信息补全占位状态，避免旧模型再次被写回待补列表
- 归档抓取、批量预扫描、订阅扫描、源端刷新等重型子进程新增 30 分钟无进度超时，避免后台任务卡死后长期占住刷新状态

### 2026-04-23
- 版本号升级到 `v0.5.79`
- 归档抓取、批量预扫描、订阅扫描和源端刷新里的重抓取逻辑改为子进程执行，进一步降低与 Web 访问的 GIL 竞争
- 本地整理调度器改为独立 daemon 子进程，目录扫描和整理调度不再留在 Web 主进程后台线程里
- 保留 `MAKERHUB_LOCAL_ORGANIZER_MODE=thread` 回退旧线程模式，`MAKERHUB_LOCAL_ORGANIZER_MODE=off` 可关闭本地整理自动启动

### 2026-04-22
- 版本号升级到 `v0.5.78`
- Web 请求侧新增独立线程池，模型库、详情页、来源库、任务页、订阅页等同步读盘接口不再直接阻塞 FastAPI 事件循环
- 归档提交/预扫描、订阅创建/同步触发、缺失 3MF 重试和现有库信息补全改走任务控制线程池，避免这些操作进行中拖慢普通页面访问
- 保留归档队列、订阅调度、源端刷新各自的后台 worker，和页面访问线程分离

### 2026-04-22
- 版本号升级到 `v0.5.77`
- 模型详情页评论区移除顶部不可用的评论输入框、图片选择、发布按钮和左侧“全部”筛选按钮
- 评论区排序入口保留“热门 / 最多点赞 / 最新 / 最多回复”，减少与 MakerWorld 展示不一致的无效控件

### 2026-04-22
- 版本号升级到 `v0.5.76`
- 打印配置时长解析补充兼容实例级标量 `prediction` 字段，旧库里即使 `printTimeSeconds` 为空，也能直接恢复打印列表和悬浮窗里的打印时长
- 归档整理链路同步把该时长写回 `profileDetails.printTimeSeconds`，后续新归档和补全后的模型不再遗漏这类时长数据

### 2026-04-22
- 版本号升级到 `v0.5.75`
- 打印配置耗材重量解析继续收紧：支持源端返回的同长度真实耗材重量数组，并把这些值写入对应 PLA、PETG 等材料
- 前端耗材标签不再从总耗材重量均分推算，只有源端真实提供单项重量时才显示 `g` 数
- 分盘或配置总耗材重量只用于悬浮窗“消耗耗材”总量，不再误写到某个材料标签后面

### 2026-04-22
- 版本号升级到 `v0.5.74`
- 打印配置详情 schema 继续升级到新版本，旧库里虽然已带旧 schema 号、但仍缺少耗材重量和喷嘴字段的模型，现在也能再次被“现有库信息补全”扫描并重新入队
- 前端在检测到运行中的 MakerHub 版本变化后会自动整页刷新一次，避免网页更新完成后侧边栏版本号已变化、但详情页仍停留在旧 JS 资源

### 2026-04-22
- 版本号升级到 `v0.5.73`
- 模型详情页打印配置列表继续对齐 MakerWorld：列表项只保留打印时间、盘数和评分，并把评分星标调整为黄色
- 打印配置悬浮窗信息改为展示盘数、打印时间、喷嘴直径和耗材消耗，更接近 MakerWorld 当前布局
- 兼容旧归档里的 `usedWeight`、`consumption`、`filamentWeight` 等耗材字段，恢复 `PLA`、`PETG` 等耗材标签后的重量 `g` 数显示

### 2026-04-22
- 版本号升级到 `v0.5.72`
- 现有库信息补全入口修复：旧模型即使打印配置详情 schema 已是最新，只要缺少实例图片、缩略图或分盘缩略图，也会被重新扫描并加入补全队列
- 设置页“现有库信息补全”文案调整为“扫描并入队”，明确实际补全会继续在后台归档队列执行，避免误以为按钮点击后会立刻补完

### 2026-04-22
- 版本号升级到 `v0.5.71`
- 模型详情页评论区继续按 MakerWorld 收口：移除多余操作元素，回复默认只展开 3 条，并在列表底部支持自动加载更多评论
- 模型详情页头部与右侧打印配置面板继续对齐 MakerWorld：来源面包屑移到标题行右侧，打印配置卡高度与主图保持一致
- 打印配置悬浮窗交互改为更接近 MakerWorld：悬浮窗只保留缩略图条，点击缩略图会直接弹出大图预览

### 2026-04-22
- 版本号升级到 `v0.5.70`
- 模型详情页评论区重做为更接近 MakerWorld 的展示：补上只读评论输入框外观、排序栏、评分星级、标签、点赞/回复操作栏和嵌套回复卡片
- 评论数据标准化补充点赞数、回复数、评分、标签和子回复透传，详情页评论展示不再局限于作者、时间、正文和图片
- 模型详情页打印配置头部信息继续收口：顶部重复的“当前打印配置”区域移除，时长、盘数、下载量和评分改为统一图标样式

### 2026-04-22
- 版本号升级到 `v0.5.69`
- 打印配置详情补全链路修复：恢复嵌套耗材 payload 解析，并提升打印配置详情 schema 版本，让旧库也能通过现有补全入口回填耗材颜色、重量、AMS 等字段
- 模型详情页打印配置展示继续收口：耗材颜色标签和相关图标信息只保留在悬浮详情窗里，顶部当前配置区域不再重复展示
- 打印配置评分显示统一按 MakerWorld 五分制处理，修复详情页和模型库把比例值或长小数直接显示成 `0.4`、`1.0`、`0.9619...` 的问题

### 2026-04-22
- 版本号升级到 `v0.5.68`
- 首页国内站 / 国际站状态卡现在可直接跳转到对应 MakerWorld 官方站点，方便手动核对 Cookie 和网页状态
- 当源端状态为“需要验证”时，状态卡会明确显示“去验证”入口，点击即可打开对应站点完成浏览器验证后再回 MakerHub 刷新检测

### 2026-04-22
- 版本号升级到 `v0.5.67`
- 源端刷新保留默认全库可刷新模型范围，不额外增加批量上限；刷新单个模型时改为轻量路径，跳过图片下载、`3MF` 直接获取和归档页面重建，降低全库刷新耗时
- 源端刷新中新发现的打印配置会进入新增 `3MF` 下载队列，不再混入缺失 `3MF` 重试列表；到达每日上限时仍会标记为待后续补取

### 2026-04-22
- 版本号升级到 `v0.5.66`
- 归档线程新增打印配置详情整理：记录盘数、打印时长、喷嘴直径、耗材总重、AMS 状态以及耗材材料、颜色和重量
- 设置页系统区域新增“现有库信息补全”入口，可手动补齐旧库缺失的打印配置详情字段，补全模式不会主动下载 `3MF`
- 模型详情页新增 MakerWorld 风格的打印配置图标和耗材颜色标签展示，订阅、单模型和源端刷新归档后的模型统一由归档线程整理这些字段

### 2026-04-21
- 版本号升级到 `v0.5.65`
- 订阅页改为默认展示来源卡片，右上角提供“添加订阅”和“订阅管理”两个入口；原订阅链接明细、编辑、立即同步和删除操作迁移到独立订阅管理页
- 新增订阅改为弹窗填写链接、名称和 Cron，创建后会立即扫描来源并启动首次同步，不再需要手动点击“立即同步”
- 侧边栏版本区改为更及时的独立刷新，并修正侧边栏头部按钮和底部版本信息溢出；导航栏“设置”移动到“日志”上方

### 2026-04-21
- 版本号升级到 `v0.5.64`
- 全项目时间统一固定为 `Asia/Shanghai`：后端调度、状态、日志、会话和归档时间统一按中国时间写入和解析，新时间戳统一带 `+08:00`
- 前端时间渲染统一按中国时间显示，旧的无时区历史数据也按中国时间解释，避免首页、订阅、源端刷新页再次出现 `0` 点显示成 `8` 点

### 2026-04-21
- 版本号升级到 `v0.5.63`
- 修复源端刷新状态恢复逻辑：如果服务是在刷新过程中重启，启动后会自动识别并清掉遗留的“运行中”状态，重新恢复为空闲并安排下一次执行
- 修复源端刷新时间显示偏移：前端不再把服务端本地时间误当成 UTC 解析，`0` 点计划时间会正确显示为 `00:00`

### 2026-04-21
- 版本号升级到 `v0.5.62`
- 源端刷新默认调度时间改为每天 `0` 点；首次启动或保存设置后也不再立即跑一轮，而是按 Cron 计算到下一次计划执行时间
- 归档生成页在离线模板资源不存在时改为只提示一次并继续使用内置回退 HTML，同时任务进度文案从“正在生成离线页面”调整为“正在生成归档页面”，避免误判成异常

### 2026-04-21
- 版本号升级到 `v0.5.61`
- 归档日志优化：请求 Cookie 头改为脱敏摘要输出，不再把 token / refreshToken 直接写进日志；Cloudflare 验证页检测也改成更严格的特征匹配，减少正常页面误报
- 首页、模型库和模型详情页补做性能优化：订阅“源端删除”状态索引改为按配置 / 状态文件版本缓存复用，避免每次请求都重建整套索引
- 侧边栏“当前版本 / 最新版本”改为独立轻量接口定时刷新，只更新版本区域本身，不再连带触发整页首页数据刷新

### 2026-04-21
- 版本号升级到 `v0.5.60`
- 订阅同步和源端刷新在对应站点已到达 3MF 每日下载上限时，不再继续请求 3MF 下载接口，只刷新模型图片、评论等元数据并把缺失打印配置加入缺失 3MF 队列
- 3MF 下载探测一旦识别到每日上限会立即停止后续候选接口尝试，避免日志持续刷 404、登录失败或 Cloudflare 回退请求

### 2026-04-21
- 版本号升级到 `v0.5.59`
- 作者订阅来源卡头像改为直接使用该订阅已归档模型里保存的作者头像，不再依赖作者来源页头像抓取

### 2026-04-21
- 版本号升级到 `v0.5.58`
- 订阅作者来源卡没有单独抓到作者页头像时，会从该订阅已归档模型的作者头像里自动回填，避免 MakerWorld 作者页验证导致来源卡一直显示占位头像

### 2026-04-21
- 版本号升级到 `v0.5.57`
- 首页国内站 / 国际站健康检查和设置页 Cookie 测试改为共用同一套认证探针，避免两边出现一个提示 Cookie 失效、另一个测试成功的不一致
- Cookie 认证探针不再额外注入从 Cookie 里解析出的 token 请求头，只按浏览器 Cookie 方式检测，避免过期 token 干扰有效 Cookie 的判断

### 2026-04-21
- 版本号升级到 `v0.5.56`
- 侧边栏顶部“隐藏”按钮下移，避免和新 logo 顶部视觉对齐过高

### 2026-04-21
- 版本号升级到 `v0.5.55`
- 修复 `/favicon.ico` 路由返回类型标注导致 FastAPI 启动时把 `FileResponse | Response` 误解析为 Pydantic response model 的问题，容器可正常启动

### 2026-04-21
- 版本号升级到 `v0.5.54`
- 接入 makerhub 项目 logo：README 顶部、浏览器标签页 favicon、侧边栏品牌位和登录页品牌位统一使用新图标

### 2026-04-21
- 版本号升级到 `v0.5.53`
- 新增订阅和每次订阅同步后都会刷新对应来源卡元数据：作者头像、粉丝数、获赞数和源端模型数会随订阅任务更新
- 订阅来源里的合集 / 收藏夹卡片改成和作者卡一致的头部信息布局，名称、头像 / 封面和站点标识统一显示在卡片上方

### 2026-04-21
- 版本号升级到 `v0.5.52`
- 新增订阅或修改订阅链接后，初始化扫描完成会立刻安排一次校验同步；如果首次初始化只扫到部分模型，后台会自动补扫新增模型，不必等下一次 cron 或手动点“立即同步”
- 订阅初始化现在写入业务日志，记录初始化扫描数量、站点返回总数、扫描页数和扫描模式，方便排查订阅源漏扫

### 2026-04-21
- 版本号升级到 `v0.5.51`
- 首页国内站 / 国际站健康检查不再调用真实 `3MF` 下载链接接口，避免刷新首页消耗 MakerWorld 每日下载链接额度
- 每日上限状态继续只读取本地 `three_mf_limit_guard.json`，过了 `Asia/Shanghai` 次日 00:00 会自动清除并恢复认证接口探测

### 2026-04-21
- 版本号升级到 `v0.5.50`
- 修复首页 3MF 下载状态的每日上限恢复逻辑：每日上限现在按 `Asia/Shanghai` 自然日计算，过期后会写回清理状态文件并重新探测，不再跨天继续显示“到达每日上限”

### 2026-04-21
- 版本号升级到 `v0.5.49`
- 模型库恢复为全部模型卡片流，重新使用来源 / 标签 / 排序筛选浏览完整模型库
- 订阅页改成三段式：顶部展示订阅来源卡片和本地状态卡片，下面保留订阅管理列表，并新增独立“订阅设置”
- 取消容器启动时的来源卡全库元数据补抓，不再因为来源卡展示而在启动阶段额外扫描整库

### 2026-04-21
- 版本号升级到 `v0.5.48`
- 模型库来源分组页顶部标题区样式微调：标题和副标题改成更紧凑的单行展示，避免深色模式下头图区文案显得过高、过松

### 2026-04-20
- 版本号升级到 `v0.5.47`
- 设置页进入“系统”标签时现在会强制刷新 GitHub 最新版本与更新日志，不再因为前端保留旧缓存值而继续显示上一个版本号

### 2026-04-20
- 版本号升级到 `v0.5.46`
- 修复模型库来源卡片页的深色模式适配：来源卡、作者区、统计块、占位图和分组页头现在会跟随深色主题切换，不再显示浅色白块

### 2026-04-20
- 版本号升级到 `v0.5.45`
- 模型库改成 MakerWorld 风格的一级来源卡片页，首页先展示作者 / 合集 / 收藏夹 / 本地整理，以及本地收藏 / 已打印 / 源端删除 / 本地删除这几类卡片
- 点击来源卡片后进入对应分组模型列表，再点模型进入详情页；作者卡、合集卡和状态卡统一按 MakerWorld 卡片样式展示
- 新增来源卡元数据后台补抓：容器启动后会自动补一轮作者、合集、收藏夹卡片的头像、封面和统计信息，不阻塞首页正常打开

### 2026-04-20
- 版本号升级到 `v0.5.44`
- 合并归档性能优化到主线：补充归档阶段耗时日志，减少评论提取的大对象递归扫描，缓存离线页模板资源，并把归档完成后的模型库快照更新改成优先增量写回
- 继续压低单次归档的 CPU 热点：`__NEXT_DATA__` 解析新增快路径，实例文件名分配和归档目录整理减少重复扫描，在线更新后即可直接获得这一批优化

### 2026-04-20
- 版本号升级到 `v0.5.43`
- 新增设置页“系统更新中心”：集中显示当前版本、线上版本、最近更新日志与更新状态，并支持在页面中直接触发更新
- Docker Compose 部署现在默认挂载 `/var/run/docker.sock`，设置页可直接调用 Docker API 拉取最新镜像并重建当前容器
- 网页一键更新的容器替换流程改为“先创建临时新容器，再切换，再清理旧容器”；如果新容器启动失败，更新 helper 会尽量自动回滚到旧容器
- README 与部署说明同步补充系统更新能力介绍、Compose 默认配置和手动更新命令

### 2026-04-20
- 版本号升级到 `v0.5.42`
- 归档和订阅现在兼容作者主页根链接：如果输入 `https://makerworld.com.cn/zh/@user_xxx` 这类作者页地址，会自动补成 `.../upload` 再执行批量归档或订阅

### 2026-04-20
- 版本号升级到 `v0.5.41`
- 模型卡片新增 MakerWorld 风格的打印配置摘要，直接显示代表配置的打印时长、盘数和评分
- 模型详情页顶部的当前打印配置信息已同步强化，补上更醒目的时长 / 盘数 / 评分信息，并在配置预览卡里加入打印时长

### 2026-04-20
- 版本号升级到 `v0.5.40`
- 修复 `v0.5.39` 的本地整理记录数回填遗漏：即使旧状态已经被误标记为 `count_trusted = true`，现在仍会在命中 50 条显示上限时回查 `organizer.log`，并把真实总数重新写回状态文件

### 2026-04-20
- 版本号升级到 `v0.5.39`
- 修复旧本地整理状态文件的记录数回填问题：当历史状态已经被截成 50 条时，现在会从 `organizer.log` 回填真实总数，不再继续把旧的 50 当成总记录数

### 2026-04-20
- 版本号升级到 `v0.5.38`
- 修复本地整理页“记录数”统计口径错误：记录数现在显示真实总数，不再因为列表只保留最近 50 条而长期固定显示为 50

### 2026-04-20
- 版本号升级到 `v0.5.37`
- 新增 Cookie 请求头规范化：自动清理复制粘贴时混入的换行、控制字符和 `Cookie:` 前缀，避免首页源端状态、归档和源端刷新因为 `Invalid header value` 直接报连接异常

### 2026-04-20
- 版本号升级到 `v0.5.36`
- 首页新增国区 / 国际区源端状态指示，可直接显示正常、需要验证、Cookie 失效或到达每日上限
- 3MF 获取失败原因改为源端感知提示，优先保留更准确的失败原因，减少被笼统显示为“未获取”

### 2026-04-19
- 版本号升级到 `v0.5.35`
- 首页顶部说明文案与“归档任务 / 订阅 / 源端刷新 / 本地整理”总标题已移除，工作台首屏更干净
- 全项目展示文案统一将“远端刷新”更名为“源端刷新”
- 源端刷新状态读取增加兼容转换，旧状态文件里的“远端刷新”字样也会立即按新名称显示

### 2026-04-19
- 版本号升级到 `v0.5.34`
- 首页将归档任务、订阅、源端刷新和本地整理统一为两行两列概览卡片，信息更集中
- 任务页“缺失 3MF”列表中的模型 ID 现在可直接跳转到模型详情页
- 源端刷新页与首页概览统一改为“本轮计划 / 已完成”语义，不再把计划总数误显示为已处理数量

### 2026-04-19
- 版本号升级到 `v0.5.33`
- 去掉 Cron 悬浮窗底部的表达式预览与说明，避免和输入框内容重复
- 压缩 Cron 浮层内边距、标题区和按钮区间距，使整体布局更紧凑

### 2026-04-19
- 版本号升级到 `v0.5.32`
- 修复 Cron 输入框浮层在弹窗内靠近视口底部时会超出显示区域的问题
- Cron 浮层现在会按可用空间自动改为向上展开，并限制最大高度后启用内部滚动

### 2026-04-19
- 版本号升级到 `v0.5.31`
- 所有 Cron 设置项移除独立的“可视化”按钮
- Cron 交互改为单个输入框 + 下方悬浮面板：点击或聚焦输入框后，直接在下方展开规则面板
- 原来的全屏弹窗改为非模态浮层，规则切换与时间字段修改会即时回写 Cron 输入框

### 2026-04-19
- 版本号升级到 `v0.5.30`
- 修复本地整理子进程写入新模型后，模型库列表缓存未同步失效的问题
- 归档快照失效改为同时写入共享 marker 文件，主 API 进程会在下一次读取模型列表时自动重建快照
- 修复现象包括“模型详情能打开，但模型库列表搜索不到新整理入库的本地模型”

### 2026-04-19
- 版本号升级到 `v0.5.29`
- 源端刷新改为“启动前避让、启动后跑完整轮”：开始本轮后不再因为归档队列或本地整理中途插入而提前停止
- 修复源端刷新页相关文案，不再提示“被更高优先级任务打断”，改为更直观的整轮刷新描述

### 2026-04-19
- 版本号升级到 `v0.5.28`
- 优化模型归档时的“处理实例信息”阶段：优先从当前实例载荷里直接提取 `3MF` 下载线索，不再无差别逐个实例补抓接口
- 对已存在于本地 `meta.json` 的实例下载地址、文件名和图片映射优先复用，重复归档同一模型时明显减少重复请求
- 实例图集与分盘缩略图改为先保留远端引用并复用旧本地映射，不再在元数据阶段串行下载整套实例图片，归档首轮速度显著提升
- 日志新增 `payload_hint / existing_hint / api_fetch` 统计，便于直接判断本次实例阶段到底复用了多少条线索、还剩多少条必须走接口

### 2026-04-19
- README 首页新增项目介绍与功能概览，便于首次访问仓库时快速理解 makerhub 的定位和主要能力

### 2026-04-19
- 版本号升级到 `v0.5.27`
- 全库 `3MF` 修复新增候选预筛选：优先处理“当前缺失列表中的模型”和“实例文件名与本地文件不匹配的模型”
- 对本地文件名已完全对应、且不在缺失队列中的模型直接跳过，减少无意义的全库 `3MF` 扫描
- 修复状态新增 `skipped_models`，便于区分真实候选数与被快速跳过的正常模型

### 2026-04-19
- 版本号升级到 `v0.5.26`
- 全库 `3MF` 修复不再对同一个模型重复执行两次 `resolve_model_instance_files()`，去掉构建缺失列表时的重复 `3MF` 扫描
- 修复单模型阶段的 CPU 与磁盘开销进一步下降，优先提升全库修复的推进速度

### 2026-04-19
- 版本号升级到 `v0.5.25`
- 全库 `3MF` 映射修复改为分批回写缺失状态，不再每处理一个模型就重写整份 `missing_3mf.json`
- 修复状态新增进度信息，后台执行时可以看到总模型数、已扫描数、已修复模型数和当前处理目录
- 降低修复任务与主进程并发写状态文件的冲突概率，减少长时间卡在 `running` 却迟迟不结束的情况

### 2026-04-19
- 版本号升级到 `v0.5.24`
- 源端刷新已移除“单轮数量”配置，前端不再显示该输入项
- 后端改为每轮默认扫描库内全部可刷新的远端模型，不再按固定批次截断
- 源端刷新页面与首页概览中的相关文案已同步调整为“本轮处理/剩余待补跑”

### 2026-04-19
- 版本号升级到 `v0.5.23`
- `/api/bootstrap` 的 GitHub 最新版本检查改为后台刷新：前端拿旧缓存即可立即返回，不再因为公网下读取 GitHub 失败或超时把整个页面初始化卡住
- 新增 `3MF` 每日下载上限守卫：一旦某次下载返回“达到每日下载上限”，MakerHub 会停止继续把缺失 `3MF` 重试任务塞进归档队列，并把队列里尚未执行的这类重试任务统一暂停
- 缺失 `3MF` 条目现在会保留更具体的失败原因（每日上限、登录失效、Cloudflare、源端未提供下载地址等），源端刷新重建缺失列表时也不会把这些原因冲掉

### 2026-04-19
- 版本号升级到 `v0.5.22`
- 将全库 `3MF` 修复任务从线程切换为独立子进程执行，避免 CPU 密集扫描与 JSON 写回期间占住主进程 GIL，导致首页、状态接口和其他页面继续超时卡死
- `GET /api/admin/archive/repair-3mf` 现在直接读取 `/app/state/archive_repair_status.json`，修复进行中也能保持轻量返回，不再依赖进程内内存状态
- 修复任务状态新增 `pid` 持久化与僵尸状态自愈：如果修复进程异常退出，状态文件会自动回落并标记错误信息

### 2026-04-19
- 版本号升级到 `v0.5.21`
- 将 `POST /api/admin/archive/repair-3mf` 改为后台任务模式：接口现在会立即返回，不再因为 `test.ace-station.top:1111` 的反代超时而中断全库修复
- 新增 `GET /api/admin/archive/repair-3mf` 状态接口，可查看当前是否仍在修复、开始/结束时间、最近一次修复结果与错误信息
- 修复完成后仍会写入业务日志，便于在公网部署场景下确认全库 `3MF` 映射修复是否已经真正落盘

### 2026-04-19
- 版本号升级到 `v0.5.20`
- 新增全库 `3MF` 映射修复能力：会读取每个模型目录下现有的 `.3mf` 文件，优先用 `DesignProfileId / configFingerprint / 文件名 ID / 标题` 反向匹配实例，并把被源端刷新冲掉的 `fileName` 批量修回 `meta.json`
- 新增受保护接口 `POST /api/admin/archive/repair-3mf`，登录后可以直接触发一次全库修复，同时重建缺失 `3MF` 队列
- 模型详情页和缺失 `3MF` 判定现在都接入本地 `.3mf` fallback：即使旧 `meta.json` 还没修回，详情页也会优先按本地文件真实映射显示下载状态，不再因为 `fileName` 失配把已有 `3MF` 误判成缺失

### 2026-04-19
- 版本号升级到 `v0.5.19`
- 修复源端刷新会把已有打印配置的 `3MF` 元信息冲掉的问题：当本轮刷新没拿到新的 `downloadUrl` 时，会保留本地已存在的 `fileName / downloadUrl / apiUrl`
- 源端刷新成功后会同步更新缺失 `3MF` 队列，避免详情页已经显示“3MF 未获取”，任务页计数却还是 `0`

### 2026-04-19
- 版本号升级到 `v0.5.18`
- 修复源端刷新页偶发直接显示整段 `<!DOCTYPE html>...` 源码的问题
- 后端现在会把上游返回的 HTML/风控页错误清洗成短提示，不再把整页源码写入源端刷新历史或状态
- 前端 API 错误提示也新增 HTML 响应兜底，即使上游临时返回网页源码，页面上也只显示明确的排查提示

### 2026-04-19
- 版本号升级到 `v0.5.17`
- 左侧导航栏中的 `任务` 入口文案改为 `归档任务`

### 2026-04-19
- 版本号升级到 `v0.5.16`
- 模型库卡片新增 `本地已删` 标记，与现有 `源已删除` 一起显示，便于直接区分本地软删除与源端删除

### 2026-04-19
- 版本号升级到 `v0.5.15`
- 模型库筛选框新增 `源端删除 / 本地删除` 两个选项
- `源端删除` 会筛出带源端删除标记的模型；`本地删除` 会显式显示已被 MakerHub 软删除并默认隐藏的模型

### 2026-04-19
- 版本号升级到 `v0.5.14`
- 模型删除改为 MakerHub 端软删除：不再物理删除归档目录，只写入本地删除标记
- 模型库默认隐藏已在 MakerHub 端删除的模型，首页模型总数与来源统计也同步排除这些项目
- 订阅归档、手动归档重试与本地整理现在都会识别本地删除标记，避免已删除模型再次被重新拉回库内
- 源端刷新不再处理已在 MakerHub 端删除的模型，减少无效刷新与后台负载

### 2026-04-18
- 版本号升级到 `v0.5.13`
- 简化“新增订阅”表单：默认只显示链接、名称和添加按钮
- 原“启用 / Cron / 创建时初始化当前源页面”改为“高级选项”折叠区，默认收起，减少首页级操作噪音
- 折叠区保留原有默认值：`已启用 / 每 6 小时 / 创建时初始化当前源页面`

### 2026-04-18
- 版本号升级到 `v0.5.12`
- 修复本地整理在容器重启或 worker 退出后遗留 `running` 状态导致的死锁问题
- 本地整理扫描时会把无活动 worker 的残留 `running` 任务自动回退为 `queued`，继续向后消费队列
- 增加 worker 退出后的状态兜底：如果任务仍停留在 `running`，会自动重新排队，避免一直卡在同一个 3MF

### 2026-04-18
- 版本号升级到 `v0.5.11`
- 所有前端 `Cron` 输入已接入统一弹窗向导，新增订阅、编辑订阅、源端刷新都可以通过可视化方式生成表达式
- Cron 向导支持常见的 `每分钟 / 每隔 N 分钟 / 每小时 / 每隔 N 小时 / 每天 / 每周 / 每月 / 每年 / 自定义` 场景
- 仍然保留原始 Cron 文本输入，方便继续直接粘贴复杂表达式；弹窗底部会同步显示当前表达式与中文解释

### 2026-04-18
- 版本号升级到 `v0.5.10`
- 设置页与遗留设置模板已移除 `No Proxy` 配置项，连接设置界面只保留 `启用 HTTP 代理 / HTTP Proxy / HTTPS Proxy`
- 前端保存代理配置时不再提交 `no_proxy`，后端结构暂时保留兼容，避免直接破坏已有配置文件

### 2026-04-18
- 版本号升级到 `v0.5.9`
- 设置页里的 `HTTP 代理` 开关已改成和订阅列表一致的开关按钮样式，不再使用旧的原生勾选框
- 代理启用状态现在会直接显示 `启用中 / 已停用`，并保留说明文案，交互与源端刷新、订阅页保持统一

### 2026-04-18
- 版本号升级到 `v0.5.8`
- 首页进一步精简为轻量总览，顶部只保留状态与 4 个统计卡，不再堆叠过多说明与入口
- 首页主体收敛为 `任务摘要` 与 `自动化概览` 两个主面板，减少首次进入时的信息噪音
- 订阅、源端刷新、本地整理改为更紧凑的概览卡片，需要处理细节时再进入对应页面

### 2026-04-18
- 版本号升级到 `v0.5.7`
- 首页重做为自动化工作台，新增 `订阅概览 / 源端刷新概览 / 本地整理概览` 三块一级摘要，集中展示当前自动化状态
- 首页顶部新增快捷入口，直接跳转到 `归档任务 / 订阅 / 本地整理 / 源端刷新`
- 任务摘要区同步收纳“最近失败”和“待补 3MF”，首页现在更适合先看总览，再进入各业务页处理细节

### 2026-04-18
- 版本号升级到 `v0.5.6`
- GitHub 最新版本读取现在支持复用设置页里的 HTTP 代理，不再让 DSM 容器始终裸连 GitHub
- 最新版本读取新增多级回退：`raw.githubusercontent.com` 失败后会继续尝试 `jsDelivr` 和 GitHub API
- 如果最新版本读取临时失败，会保留上一次成功读取到的版本号，不再因为瞬时网络抖动把侧栏直接打成“读取失败”
- 侧栏版本显示逻辑已调整：只要还有缓存的最新版本，就优先显示版本号；只有完全没有可用版本时才显示“读取失败”

### 2026-04-18
- 版本号升级到 `v0.5.5`
- 源端刷新配置里的“启用源端刷新”已从普通勾选框改为和订阅列表一致的开关样式，交互和视觉保持统一

### 2026-04-18
- 版本号升级到 `v0.5.4`
- 左侧导航里的 `整理` 改名为 `本地整理`
- 本地整理页右侧新增数字摘要，直接显示候选 3MF、运行中、排队中和当前记录数
- 任务页底部的“本地整理任务”区块已移到本地整理页下方，任务页保留顶部整理摘要，避免一个页面塞两套视图

### 2026-04-18
- 版本号升级到 `v0.5.3`
- 调整源端刷新页顶部 4 个统计卡的响应式布局：宽度足够时保持 4 个一行，宽度不足时改为 2 个一行
- 源端刷新历史新增筛选条，支持 `有远端更新 / 全部 / 异常与跳过` 三种视图，默认先显示真正有远端变化的记录

### 2026-04-18
- 版本号升级到 `v0.5.2`
- 修复源端刷新候选排序读取错误：源端刷新现在会正确按“最久未检查”轮转，不再长期只重复刷新同一批 `12` 个模型
- 源端刷新页新增批次摘要，直接显示可刷新总数、单轮处理数、剩余待刷数量，以及缺 Cookie / 非远端单模型的跳过数量
- 源端刷新历史改为逐次记录，不再被同一模型覆盖；现在可以看到“什么时间刷新了哪个模型，以及评论 / 打印配置 / 附件 / 简介发生了什么变化”

### 2026-04-17
- 版本号升级到 `v0.5.1`
- 将“源端刷新”从设置页内嵌表单拆成独立页面，左侧导航新增 `整理` 与 `源端刷新`
- 设置页现在只保留 `连接设置 / 通知 / 用户` 三块，避免“系统设置”和“任务策略”继续堆在同一页
- 新增独立路由 `/organizer` 与 `/remote-refresh`，公网直接访问时也能正常打开对应页面

### 2026-04-17
- 版本号升级到 `v0.5.0`
- 新增“源端刷新”能力：模型库内已有远端来源链接的模型，会按设置页里的 `Cron + 单轮数量` 分批做远端增量刷新
- 源端刷新默认启用，且会在归档队列和本地整理空闲时才运行，避免和手动归档、本地整理抢占同一批资源
- 已成功下载过的 `3MF` 不会在源端刷新时重复下载；评论会做增量合并，不会因为源端接口只返回部分评论而覆盖掉本地旧评论
- 模型级“源端删除”与打印配置级“源端删除”现在会写入 `meta.json`，模型库卡片和详情页都能看到对应标记
- 设置页“整理”标签新增“源端刷新”配置卡，可直接控制启用状态、Cron 与单轮刷新数量

### 2026-04-17
- 版本号升级到 `v0.4.18`
- 日志页 UI 重构为更接近 `Immortal` 风格的日志工作台布局
- 顶部新增日志概览状态条，显示当前加载行数、自动追踪状态和最近时间
- 左侧新增日志级别过滤区，支持按 `DEBUG / INFO / WARN / ERROR / SUCCESS` 快速筛选
- 右侧日志流改为分彩色卡片展示，日志来源、搜索、范围和级别过滤统一收纳到单页工作台

### 2026-04-17
- 版本号升级到 `v0.4.17`
- 修复缺失 `3MF` 重新下载时反复改写模型 `collectDate` 的问题
- 现有模型再次归档或重试缺失 `3MF` 时，会保留首次入库时的采集时间，不再因为重试失败把旧模型顶到模型库最前面

### 2026-04-17
- 版本号升级到 `v0.4.16`
- 设置页“连接设置”新增 3 个测试按钮：`国内 Cookie`、`国际 Cookie`、`HTTP 代理`
- 国内 / 国际 Cookie 测试会直接请求 MakerWorld 的登录态接口，快速判断当前 Cookie 是否还能用
- Cookie 测试会自动带上当前页面里填写的代理设置，便于一起排查“到底是 Cookie 失效还是代理不通”
- HTTP 代理测试会分别验证 `makerworld.com.cn` 和 `makerworld.com` 的连通性，并把结果写入 `business.log`

### 2026-04-17
- 版本号升级到 `v0.4.15`
- 新增顶层“日志”页面，侧边栏可直接进入查看业务日志
- 新增 `/api/logs` 日志读取接口，支持切换 `business.log`、`subscriptions.log`、`organizer.log`、`batch_queue.log` 等日志文件，支持搜索、行数限制和自动刷新
- 归档、批量预扫描、订阅同步、本地整理、缺失 3MF、模型删除、附件上传、登录与设置保存等关键业务动作会写入统一 `business.log`
- 日志输出会自动屏蔽 Cookie、Token、密码等敏感字段

### 2026-04-17
- 版本号升级到 `v0.4.14`
- 修复模型详情页深色模式下“打印配置”区块仍出现黑色标题文字的问题
- 深色主题下，打印配置标题、配置卡片标题和机型标签都会跟随暗色配色显示，不再出现看不清的黑字

### 2026-04-17
- 版本号升级到 `v0.4.13`
- 本地整理新增排队可见性：`local` 扫描到的递归子目录 `.3mf` 会先显示为 `queued`，不再等真正开跑后任务页才出现记录
- 任务页“本地整理”区块新增运行中/排队中计数和“当前检测到多少个候选 3MF”的提示，整文件夹导入时能立刻看出是否扫描到
- 保留递归遍历逻辑，子目录内的 `.3mf` 仍然会继续自动识别

### 2026-04-17
- 版本号升级到 `v0.4.12`
- 新增对 MakerWorld 合集详情页链接的识别与批量归档支持，例如 `https://makerworld.com.cn/zh/collections/267246-...`
- 这类合集详情页现在会直接按 `collection_id` 走分页接口抓取，不再被误判成“不支持的链接类型”
- 订阅页和任务页的链接提示文案同步更新，收藏夹订阅入口也接受合集详情页链接

### 2026-04-17
- 版本号升级到 `v0.4.11`
- 批量归档确认弹窗新增“归档并订阅”按钮，作者页和收藏夹页面现在可以在确认归档时同步自动创建订阅任务
- 订阅名称会优先从当前作者页/收藏夹页面自动提取；如果页面标题无法识别，再回退到原来的链接默认名
- `/api/archive` 新增批量归档时同步写入订阅的能力；如果该链接已经存在订阅，则会在提交归档时直接更新原订阅并刷新基线

### 2026-04-17
- 版本号升级到 `v0.4.10`
- 修复本地整理“重新把同一批 3MF 扔进 local 后没有反应”的问题：本地整理任务指纹现在会同时纳入 `ctime` 和 inode，不再因为路径、文件大小、`mtime` 与历史成功记录一致就被误判成“已处理”
- 新增本地整理跳过日志 `candidate_ignored_existing`，后续如果某个文件再次被历史记录拦截，日志里会直接显示命中的状态和消息

### 2026-04-17
- 版本号升级到 `v0.4.9`
- 收紧本地整理任务的资源占用：本地整理 worker 现在在单文件处理完成后会进入冷却期，不再无间隔连续吞掉一批 `3MF`
- 新增本地整理 worker 超时保护，单个文件处理超时会被终止并写回失败状态，避免任务页长期卡在“运行中”
- 优化本地 `3MF` 去重签名：对没有 `DesignProfileId` 的文件，优先使用 ZIP 目录元数据生成轻量签名，减少整文件 `SHA256` 带来的 CPU 与磁盘压力

### 2026-04-17
- 版本号升级到 `v0.4.8`
- 调整登录页 UI 布局：登录卡片头部改为稳定的双列结构，品牌区与登录说明分离，修复品牌标识、标题与副标题挤在一起的问题
- 新增统一的 `brand-mark__badge` 样式，登录页和侧边栏的 `MH` 品牌徽标现在都会按固定尺寸显示
- 优化登录页表单区：按钮和状态文案重新排版，窄宽度下会自动切成单列

### 2026-04-17
- 版本号升级到 `v0.4.7`
- 修复退出登录后无法看到登录页的问题：后端鉴权中间件现在允许未登录访问 `/api/bootstrap`，这样前端在 `/login` 页面初始化时不会再因为 `401 Unauthorized` 中断路由
- 修复后，退出登录再访问首页会正确跳到登录页，直接打开 `/login` 也能正常显示登录表单

### 2026-04-17
- 版本号升级到 `v0.4.6`
- 修复模型详情页点击后一直停留在“正在加载模型详情”的问题：详情页中打印配置卡片的 DOM 引用不再写入响应式状态，避免 Vue 在渲染阶段反复触发自身更新，导致页面进入假死
- 新增详情页组件级错误兜底：如果后续仍有渲染异常，页面会直接显示错误信息，而不是一直卡在加载态

### 2026-04-17
- 版本号升级到 `v0.4.5`
- 修复订阅页“下次运行/上次运行”时间显示错误：前端现在会把后端返回的无时区 ISO 时间按 UTC 解释，避免容器当前采用 UTC 时，页面把 `06:00` 误当成浏览器本地 `06:00`，从而错误显示成“过去时间”
- 这个修复会立即作用于现有线上状态文件，不需要等下一轮订阅任务重写时间字段

### 2026-04-17
- 版本号升级到 `v0.4.4`
- 修复订阅“立即同步”和任务页手动批量归档口径不一致的问题：手动触发订阅同步时，改为按“本地未归档且队列中不存在”的模型补齐入队，不再只看订阅基线差异
- 这样新建作者/收藏夹订阅后，立即同步会把当前源页面里尚未归档的模型正常加入队列；定时同步仍保留基于订阅基线的增量行为

### 2026-04-17
- 版本号升级到 `v0.4.3`
- 修复模型库进入详情页的路由入口：卡片点击与详情预览不再直接推送预编码字符串路径，统一改为按原始 `model_dir` 走命名路由，避免 Vue Router 在带 `%`、空格、中文符号的模型目录上二次解码后把详情页再次卡死
- 同步修正前端 `encodeModelPath()`，按路径分段使用 `encodeURIComponent` 编码，避免 fallback 链路继续生成脆弱 URL

### 2026-04-17
- 版本号升级到 `v0.4.2`
- 修复模型详情页长时间停留在“正在加载模型详情”的问题：详情接口不再一次性返回全部评论，首屏只返回首批评论并提供分页加载
- 新增 `/api/models/{model_dir}/comments` 分页接口，详情页“加载更多评论”改为按批次请求，避免上千条评论模型把整个页面与导航一起卡死
- 修复详情评论分页路由优先级，避免 `/{model_dir:path}` 抢占 `/comments` 子路由
- 修复详情页对模型目录与 `profileId` 哈希的解码逻辑：改为基于 `route.params` 安全解码，并统一使用 `encodeURIComponent` 请求详情接口，避免模型名或本地导入目录里包含 `%`、空格、全角符号时页面直接卡死

### 2026-04-17
- 版本号升级到 `v0.4.1`
- 优化模型详情页打开卡顿：详情对象改为浅响应，避免 Vue 对整份评论/图片树做深度代理
- 评论区改为延后分批渲染，优先显示主图、统计和打印配置，减少大评论集模型打开时的前端卡死
- 单模型详情新增后端缓存，重复打开同一模型时不再每次重新解析 `meta/comments/attachments`

### 2026-04-17
- 版本号升级到 `v0.4.0`
- 新增归档快照缓存层，首页、模型库、任务页不再每次请求都全量扫描 `/app/archive/**/meta.json`
- 首页数据装配改为复用同一份归档快照，修复 `/api/dashboard` 一次请求重复扫描模型库两轮的问题
- 批量归档状态刷新改为复用缓存中的已归档键集合，并把“仅剩批量任务待确认”时的轮询间隔从 `0.2s` 放宽到 `2s`，降低后台空转和锁竞争
- 新增归档成功、模型删除、本地整理成功后的快照失效逻辑，保证模型库和首页统计仍能在下一次请求中看到最新数据

### 2026-04-17
- 版本号升级到 `v0.3.58`
- 修复订阅调度器的到期任务入队错误：作者订阅和收藏夹订阅在轮询时不再因为 `append` 参数异常而整体停摆
- 修复后，已到执行时间的订阅会重新进入调度线程，3 个现有订阅能够继续按 cron 自动同步

### 2026-04-17
- 版本号升级到 `v0.3.57`
- 打印配置悬浮窗改为按真实可用空间动态定位：左侧空间足够时优先悬浮到左边，放不下时才切到卡片下方
- 新增配置卡宿主元素的运行时测量和窗口缩放重算，避免窄窗口下仍被固定断点强制改成下方弹层
- 保留顶部/底部卡片的边缘对齐逻辑，但仅在左侧悬浮模式下生效

### 2026-04-17
- 版本号升级到 `v0.3.56`
- 修正详情页打印配置预览的触发条件：改为按设备是否支持 `hover + fine pointer` 判断，而不是按页面宽度判断
- 窄宽度下的桌面浏览器现在也会像 MakerWorld 一样支持鼠标悬浮预览，触屏设备仍保留点击展开
- 调整配置卡点击逻辑，在支持悬浮的环境里点击只负责选中配置，不再把悬浮预览误切成点击模式

### 2026-04-17
- 版本号升级到 `v0.3.55`
- 回退详情页打印配置在窄宽度下的底部抽屉方案，恢复为贴着配置卡展开的悬浮预览
- 删除窄宽度下的遮罩层与大弹窗头部，改为点击配置卡展开、点击空白区域收起
- 统一窄宽度下悬浮预览的定位与阴影，避免手机和窄窗口继续出现整页覆盖式弹层

### 2026-04-16
- 版本号升级到 `v0.3.54`
- 继续对齐 MakerWorld 的打印配置预览交互：窄宽度下的弹层进一步改成更接近底部抽屉的样式
- 新增抽屉顶部拖拽条、吸顶头部和更贴近移动端的底部弹出尺寸，打开时使用上滑进入动画
- 深色模式补齐抽屉头部背景和关闭按钮样式，避免窄宽度下出现亮色残留

### 2026-04-16
- 版本号升级到 `v0.3.53`
- 模型详情页打印配置改为全宽度统一使用悬浮窗，不再在窄宽度下直接隐藏
- `1240px` 以下的打印配置悬浮窗改成固定弹层样式，支持点击卡片打开、点击遮罩或关闭按钮收起
- 删除移动端内联替代面板，避免和悬浮窗并存造成重复内容

### 2026-04-16
- 版本号升级到 `v0.3.52`
- 恢复模型详情页打印配置悬浮窗：悬浮窗启用条件从严格的 `1241px + hover/fine pointer` 放宽到桌面宽度即可启用
- 增加打印配置卡片点击打开悬浮窗的兜底逻辑，避免部分浏览器或远程访问环境下 hover 检测异常导致悬浮窗不出现

### 2026-04-16
- 版本号升级到 `v0.3.51`
- 修复模型详情页重复下载入口：移除打印配置悬浮层和配置摘要里的额外 3MF 按钮，页面仅保留一个主下载按钮，并始终跟随当前选中的打印配置

### 2026-04-16
- 版本号升级到 `v0.3.50`
- 修复本地整理子进程的索引退化问题：归档库索引改为落盘缓存，连续导入多个本地 3MF 时不再每个文件都重新全量扫描 `/app/archive/**/meta.json`
- 本地整理候选扫描改为可剪枝目录遍历：自动跳过 `instances`、`_duplicates`、已有 `meta.json` 的整理目录，避免 `/app/local` 存在历史目录时每轮轮询都全树硬扫
- 本地 3MF 的文件哈希改为节流读取，并且仅在缺少 `DesignProfileId` 时才计算 SHA256，降低大文件导入时的 CPU 峰值

### 2026-04-16
- 版本号升级到 `v0.3.49`
- 本地整理从站内线程切到独立子进程执行，并给整理子进程增加较低优先级，避免单个 3MF 的重计算继续卡住 Web 服务

### 2026-04-16
- 版本号升级到 `v0.3.48`
- 修复本地整理线程的高开销轮询：`local` 目录没有待处理 3MF 时不再每 5 秒全量扫描整个归档库
- 本地整理增加归档索引缓存：处理连续多个本地文件时复用内存索引，不再每个文件都重新读取全库 `meta.json`

### 2026-04-16
- 版本号升级到 `v0.3.47`
- 本地整理从“单轮最多 5 个”进一步收紧为“严格串行一次只处理 1 个 3MF”，避免大量本地文件同时导入时继续把服务拖死

### 2026-04-16
- 版本号升级到 `v0.3.46`
- 左侧导航底部“最新版本”改为可点击外链，点击后直接跳转到 makerhub 的 GitHub 仓库
- 本地整理增加单轮处理上限：每次轮询最多只领取 `5` 个本地 3MF，避免一次性投放大量文件时把应用拖死

### 2026-04-16
- 版本号升级到 `v0.3.45`
- 修复详情页响应式覆盖冲突：移除 `1480px` 断点里遗留的旧双栏宽度规则，桌面端右侧信息栏保持贴在主图右侧，不再被错误压到主图下方

### 2026-04-16
- 版本号升级到 `v0.3.44`
- 修正详情页双栏换行断点：不再在 `1240px` 就把右侧信息栏压到主图下方，改为仅在 `860px` 以下才切成上下布局

### 2026-04-16
- 版本号升级到 `v0.3.43`
- 调整模型详情页桌面端双栏贴合度：右侧信息栏不再被 `space-between` 拉到最右，主图与右栏间距从 `24px` 收窄到 `16px`

### 2026-04-16
- 版本号升级到 `v0.3.42`
- 进一步按 MakerWorld 桌面端主图尺寸收口：详情页主图区改为 `499px` 宽，主图盒子改为固定 `499×374`

### 2026-04-16
- 版本号升级到 `v0.3.41`
- 收窄模型详情页桌面端主图区尺寸：主图列宽从 `820px` 下调到 `756px`，主图下方缩略图同步缩小一档

### 2026-04-16
- 版本号升级到 `v0.3.40`
- 本地整理任务列表改为默认只显示 5 条，超过部分通过“加载更多”继续展开
- 本地整理任务接口默认按 `updated_at` 倒序返回，页面显示顺序改为最近归档的记录在最上面

### 2026-04-16
- 版本号升级到 `v0.3.39`
- 修复任务页顶部“整理任务”统计口径错误：本地整理完成后，顶部摘要不再把历史成功记录算进当前任务数
- 任务页顶部摘要文案调整为“整理中”，与下方“本地整理任务”历史记录列表区分

### 2026-04-16
- 版本号升级到 `v0.3.38`
- 左侧导航版本区把“GitHub”文案改成“最新版本”
- 远端版本读取失败时不再保留旧成功结果，侧栏会直接显示“读取失败”，避免网络超时场景下误显示旧版本号

### 2026-04-16
- 版本号升级到 `v0.3.37`
- 修复 DSM 侧 “GitHub 最新版本” 超时问题：读取策略改成 `raw.githubusercontent.com` 失败后自动回退到 `api.github.com`，并放宽超时时间，提高侧栏远端版本显示成功率

### 2026-04-16
- 版本号升级到 `v0.3.36`
- 修复“GitHub 最新版本”读取失败：避免 `urllib.request.Request` 与 FastAPI 的 `Request` 命名冲突，侧栏现在可正常读取仓库 `VERSION`

### 2026-04-16
- 版本号升级到 `v0.3.35`
- 左侧导航底部版本区改成同时显示“当前版本 / GitHub 最新版本”，后端会缓存读取仓库 `main` 分支的 `VERSION` 文件，方便直接看线上实例是否落后于 GitHub

### 2026-04-16
- 版本号升级到 `v0.3.34`
- 调整模型详情页桌面端主图区尺寸：主图列改为接近 MakerWorld 的固定宽度档位，右侧信息栏同步收窄，让主图显示尺寸与目标站更接近

### 2026-04-16
- 版本号升级到 `v0.3.33`
- 调整模型详情页头部布局：把标题、作者和来源路径从右侧信息栏挪到左侧主图区顶部，放到主图上方，右侧只保留统计、下载和打印配置

### 2026-04-16
- 版本号升级到 `v0.3.32`
- 加固本地 3MF 整理的“同模型匹配”逻辑：在 `DesignModelId` 之外，新增标题/作者的宽松别名匹配与唯一标题回退匹配，减少因为作者名或标点差异导致的新建重复 `LOCAL_*` 目录
- 本地整理命中现有模型后，仍继续沿用“同配置跳过、不同配置并入 `instances/`”的策略；这轮重点是提高命中现有模型目录的概率

### 2026-04-16
- 版本号升级到 `v0.3.31`
- 继续收紧模型详情页“打印配置”比例：缩小右侧栏宽度、配置卡缩略图、卡片间距和悬浮窗尺寸，让整体更接近 MakerWorld 的紧凑布局
- 修正中等窄宽度下详情页仍残留旧竖排图集规则的问题；现在宽度缩小时，详情页会按统一的横向图集和更紧凑的打印配置布局收口

### 2026-04-16
- 版本号升级到 `v0.3.30`
- 模型详情页主图区改成“主图在上、模型图集在主图下方”，不再使用左侧竖排图集
- 右侧信息顺序调整为“标题信息 -> 当前配置/打印配置 -> 下载与统计”，把打印配置整体提到下载/点赞/收藏/评论/打印统计上方

### 2026-04-16
- 版本号升级到 `v0.3.29`
- 修正模型详情页媒体分层错误：主图区不再接入打印配置的 `P1 / P2 / P3` 媒体，主图继续只显示模型图集
- 选中打印配置时不再把主图切成配置图/分盘图；`P1 / P2 / P3` 与配置图集恢复只在打印配置悬浮窗内查看

### 2026-04-16
- 版本号升级到 `v0.3.28`
- 模型详情页把 `P1 / P2 / P3` 配置媒体切换从右侧当前配置摘要移到主图下方，桌面端布局改得更接近 MakerWorld 的主图区结构
- 窄宽度下主图下方的配置媒体条改为横向滚动，避免和主图缩略图挤在一起

### 2026-04-16
- 版本号升级到 `v0.3.27`
- 本地 3MF 整理新增“与模型库查重”能力：会先解析 3MF 内的 `DesignProfileId / DesignModelId`，若命中模型库现有配置则直接判重跳过，不再重复导入
- 本地 3MF 整理新增“并入现有模型”能力：若命中已有模型但属于新配置，则不再新建 `LOCAL_*` 目录，而是直接补充到现有模型目录的 `instances/` 中
- 新整理导入的本地模型/实例会额外写入 `designModelId / designProfileId / fileHash / configFingerprint` 元数据，为后续跨批次查重提供稳定索引

### 2026-04-16
- 版本号升级到 `v0.3.26`
- 任务页“本地整理任务”区新增“清空记录”按钮，并补充后端清空接口；以后清历史整理任务记录直接走应用 API，不再依赖 DSM 文件覆盖

### 2026-04-16
- 版本号升级到 `v0.3.25`
- 在 README 开头补充项目来源说明：明确 makerhub 基于 `mw_archive_py` 的抓取思路二次重构，并向原作者 `sonicmingit` 的开源工作致谢

### 2026-04-16
- 版本号升级到 `v0.3.24`
- 修复首页“待补 3MF”摘要计数错误：首页继续只预览前 5 条待补任务，但计数改为显示真实总数，不再因为摘要截断而固定显示 5

### 2026-04-16
- 版本号升级到 `v0.3.23`
- 调整本地整理落盘策略：`/app/local` 只作为待整理 3MF 的监测入口，整理完成后的模型目录直接写入主模型库根目录 `/app/archive/LOCAL_*`，不再放回 `local` 子目录
- 修复 DSM 上“本地整理”递归自我导入的问题：整理器现在会跳过自己生成的 `model_dir/instances/*.3mf` 和带 `meta.json` 的受管目录，不再把已整理好的 3MF 再次当作新文件重复整理
- 线上已先临时停用本地整理扫描，避免旧版本继续扩散重复目录；新版本部署后会把整理入口恢复到 `/app/local -> /app/archive`

### 2026-04-16
- 版本号升级到 `v0.3.21`
- “本地整理任务”从仅有配置和页面骨架，补成实际可运行的后台整理器：服务启动后会持续扫描 `/app/local` 中新增的 `.3mf` 文件，并按“一模型一目录”整理到 `/app/archive/local/...`
- 本地整理会为导入的 3MF 自动生成 `meta.json`、实例文件记录和可用预览图；整理成功后，模型库与首页会跟随事件流自动刷新，不需要手动刷新页面
- 任务页的“本地整理任务”区改为显示具体文件、模型目录和状态消息，并修复多条整理任务因为 key 冲突导致显示异常的问题

### 2026-04-16
- 版本号升级到 `v0.3.20`
- 订阅卡片里的“启用”勾选框改成即时切换的开关 UI，点击即可直接启用/停用该条订阅
- 订阅卡片移除内联编辑表单，改成“编辑”按钮弹窗；弹窗内可修改订阅链接、名称和 Cron
- 后端订阅更新接口新增 `url` 支持，修改订阅链接时会重新校验链接类型，并按新链接重新初始化订阅状态

### 2026-04-16
- 版本号升级到 `v0.3.19`
- 继续按 MakerWorld 详情页重排顶部结构：改成“左侧主图 + 右侧标题信息 / 主下载按钮 / 当前配置摘要 / 打印配置列表”的双栏布局，主图缩略图改为更接近目标站的左侧竖排缩略图
- 详情内容区新增分段导航，并在描述区补回标签墙，让整体章节组织更接近 MakerWorld 详情页

### 2026-04-15
- 版本号升级到 `v0.3.18`
- 继续按 MakerWorld 详情页行为收口“打印配置”悬浮层：浮层顶部不再只显示单张预览，改为直接使用实例自带的多张配置图和分盘图，支持在浮层内切换 `图1/图2/图3/P1...`
- 每个打印配置浮层独立记住当前查看的媒体项，桌面端 hover/focus 时默认落到该配置的首张媒体，并保留 3MF 状态、统计信息和放大预览入口

### 2026-04-15
- 版本号升级到 `v0.3.17`
- 模型详情页“打印配置”区域改成更接近 MakerWorld 的配置卡 + 独立悬浮浮层交互：桌面端 hover / focus 每个配置时会单独弹出该配置的 3MF 状态与统计浮层
- 详情页新增 `#profileId-实例ID` hash 选中能力，直接打开带 hash 的详情链接时会定位并切换到对应打印配置；窄宽度和非 hover 设备仍自动回退到原来的内联详情块

### 2026-04-15
- 版本号升级到 `v0.3.16`
- 修复订阅页“新增订阅”动作区里“添加订阅”按钮与“创建时初始化当前源页面”开关在中等宽度下互相挤压重叠的问题
- 新增订阅动作区改成可收缩的两列布局，并在较窄宽度下自动堆叠；初始化开关文案允许正常换行，提交按钮改为独立对齐容器

### 2026-04-15
- 版本号升级到 `v0.3.15`
- 修复任务页“待重新下载 / 本地整理任务”表格在深色模式下仍使用浅色半透明背景的问题，表格容器与行背景现在统一跟随主题变量
- 表格正文补上主题文字色、交替行背景和末行边框收口，避免暗色模式下出现灰白块与分隔线残留

### 2026-04-15
- 版本号升级到 `v0.3.14`
- 针对 Safari 下中文在订阅创建表单操作区被压成单字竖排的问题，新增专用文案节点并强制设为不可拆行的水平排版
- 订阅创建表单第二行操作区从 `flex` 改成专用两列网格：左侧固定为初始化开关，右侧固定为提交按钮，避免再被中文最小内容宽度算法挤坏

### 2026-04-15
- 版本号升级到 `v0.3.13`
- 修复订阅页新增表单布局仍被通用 `flex` 规则覆盖的问题，紧凑版表单现在强制使用专用网格布局
- 第二行“创建时初始化当前源页面”操作区补上整行宽度和最小宽度约束，避免中文文案再被压成竖排

### 2026-04-15
- 版本号升级到 `v0.3.12`
- 修复订阅页“新增订阅”表单布局错位：改成稳定的两段式布局，第一行放链接/名称/Cron/启用，第二行只放“创建时初始化当前源页面”和提交按钮
- 订阅开关补上更明确的水平排版约束，避免长文案在中等宽度下被挤成竖排

### 2026-04-15
- 版本号升级到 `v0.3.11`
- 模型详情页“文档”区新增手动附件上传：支持直接给当前模型补传组装图、PDF、压缩包等文件，并写入模型目录侧边附件清单
- 手动上传的附件会并入详情页文档列表，图片附件支持预览，手动补传的附件支持在详情页直接删除

### 2026-04-15
- 版本号升级到 `v0.3.10`
- 继续按 MakerWorld 详情页 HAR 收口模型详情页：标题区改成更接近目标页的头像/标题/作者/面包屑结构，统计区改成目标页风格的图标+数字横排
- 打印配置区补回实例图片/分盘媒体切换入口，描述区图片与排版节奏改得更接近目标页，评论区改成更接近目标页的头像左列 + 自适应图片宫格布局

### 2026-04-15
- 版本号升级到 `v0.3.9`
- 继续按 MakerWorld HAR 收口模型详情页：右侧打印配置列表改成更接近目标站的缩略图 + 标签胶囊 + 紧凑元信息排版
- 评论区从后台卡片式改成列表分隔样式，文档区改成接近目标站的居中下载按钮布局，减少“管理后台”观感

### 2026-04-15
- 版本号升级到 `v0.3.8`
- 模型详情页从“继续压 CSS”改为按 MakerWorld HAR 重排结构：标题区拆成头像/标题/作者行和分类标签行，主内容改成左侧主视觉 + 右侧打印配置的双栏骨架
- 打印配置列表补回接近目标站的缩略图 + 文本信息卡片，主图保持 `4:3` 比例，评论卡、文档区和暗色模式细节同步收紧

### 2026-04-15
- 版本号升级到 `v0.3.7`
- 参考目标详情页 HAR 调整模型详情页整体比例：主容器收窄、标题与卡片尺寸缩小、右侧打印配置栏改窄
- 详情主图从正方形改为横向主视觉，缩略图、统计条、下载按钮和文档卡同步缩一档，整体观感更接近 MakerWorld

### 2026-04-15
- 版本号升级到 `v0.3.6`
- 归档入口移除“同时创建订阅”和 Cron 相关内容，任务页现在只负责提交归档任务
- 后端归档接口同步移除顺手创建订阅的分支，订阅只能在订阅页内单独创建和管理

### 2026-04-15
- 版本号升级到 `v0.3.5`
- 继续压缩任务页“待重新下载”列表的行高：状态说明与操作按钮区更紧凑，减少单行纵向占用
- 修复模型库卡片高度不一致的问题：卡片恢复等高布局，并把底部操作区固定在卡片底部，避免出现参差不齐的卡片高度

### 2026-04-15
- 版本号升级到 `v0.3.4`
- 优化任务页“归档入口”布局：归档链接输入、订阅开关和 Cron 输入改为更稳定的两段式排版
- 修复“作者页 / 收藏夹归档时同时创建订阅”文案在窄宽度和桌面宽度下被挤成竖排的问题

### 2026-04-15
- 版本号升级到 `v0.3.3`
- 任务页的运行中 / 排队中 / 最近失败 / 待重新下载 4 个列表默认最多显示 5 条，避免长队列把页面直接拉满
- 以上 4 个列表在超过 5 条时新增“加载更多”按钮，按 5 条一组继续展开

### 2026-04-15
- 版本号升级到 `v0.3.2`
- 模型详情页的下载 / 点赞 / 收藏 / 评论 / 打印统计改成图标化显示，风格向 MakerWorld 详情页靠齐
- 修复部分实例 3MF 尚未落地时仍暴露下载链接的问题：现在只有本地文件真实存在才会返回下载地址
- 3MF 未获取完成时，详情页会改为禁用态提示，不再点出 `{\"detail\":\"Not Found\"}`；提示文案会引导去任务页执行缺失 3MF 重新下载

### 2026-04-15
- 版本号升级到 `v0.3.1`
- 订阅页顶部总览移除“源端删除标记”统计，避免把删除状态误解成订阅页自身指标
- “新增订阅”卡片改为更紧凑的布局，压缩标题区、表单区和提示区高度，减少页面首屏占用
- 模型详情页补充“源已删除”标记，和模型库卡片保持一致；当订阅源删除该模型时，只在本地界面标记，不删除本地归档

### 2026-04-15
- 版本号升级到 `v0.3.0`
- 新增顶层“订阅”页面，与首页 / 模型库 / 设置 / 任务同级；支持创建、编辑、删除、手动同步作者页和收藏夹订阅
- 归档作者页 / 收藏夹时新增“同时创建订阅”入口，可在确认提交时一并写入订阅任务并保存 Cron
- 后端新增订阅调度器：按 Cron 定时扫描订阅源，只把新增模型加入归档队列，不重复回补已跟踪模型
- 订阅同步会记录源页面当前模型列表；如果目标作者页 / 收藏夹后续删除了某些模型，模型库会对本地已归档模型标注“源已删除”，但不会跟随远端删除本地文件

### 2026-04-15
- 版本号升级到 `v0.2.49`
- 修复批量归档确认提交后队列状态会被 `/api/tasks` 轮询覆盖的问题，避免出现“预扫描 238，提交后实际只归档少量模型且失败列表为空”的假完成
- 归档队列、缺失 3MF、模型标记等 JSON 状态写入改成进程内串行更新，降低批量入队和前端轮询同时发生时的丢任务风险

### 2026-04-15
- 版本号升级到 `v0.2.48`
- 修复收藏夹聚合页 `/@user/collections/models` 的扫描结果被空 `hits` 节点误判的问题，`/favorites/designs/{uid}` 现在会优先选择真正的模型命中结果
- 收藏夹分页接口新增 payload 摘要日志，便于定位 `hits/total` 结构；实测 `https://makerworld.com.cn/zh/@s450586793/collections/models` 现已可稳定扫描到 `238` 个模型

### 2026-04-15
- 版本号升级到 `v0.2.47`
- 收藏夹批量扫描请求头继续向 MakerWorld 前端对齐：补上 `X-BBL-Client-Name: MakerWorld`
- 采集会话的 `User-Agent` 改成浏览器风格字符串，避免 `python-requests` 风格请求在收藏夹分页接口上被降级为空结果

### 2026-04-15
- 版本号升级到 `v0.2.46`
- 修复收藏夹批量扫描没有带上官方前端同样的 `handle` 路由参数，导致 `/favorites/designs/{uid}` 虽然返回 `200` 但结果为空的问题
- 收藏夹分页请求现在会附带 `handle` 和原始链接里的相关查询参数，尽量贴近 MakerWorld 前端实际请求形态

### 2026-04-14
- 版本号升级到 `v0.2.45`
- 收藏夹批量扫描新增多候选接口探测：除了 `/favorites/designs/{uid}`，现在也会尝试 `/favorites/{uid}/designs`
- 收藏夹页的 `__NEXT_DATA__` 关键摘要会写入日志，便于定位 `userInfo.uid`、`designsData.total` 与接口返回不一致的问题

### 2026-04-14
- 版本号升级到 `v0.2.44`
- 修复 `/@user/collections/models` 在收藏接口返回空结果时直接提前退出的问题
- 收藏夹批量扫描现在会在 API `hits=0` 时自动回退到 HTML 扫描，避免把可见收藏页误判成“没有扫描到可归档模型”

### 2026-04-14
- 版本号升级到 `v0.2.43`
- 修复 `/@user/collections/models` 收藏夹拥有者 UID 解析错误的问题，不再把收藏夹里模型作者的 UID 误当成收藏夹所有者 UID
- 收藏夹批量扫描新增基于页面自身 `__NEXT_DATA__` 的 owner UID 解析链路，并禁用对收藏夹场景有副作用的 sample-model 投票兜底

### 2026-04-14
- 版本号升级到 `v0.2.42`
- 修复 `/@user/collections/models` 收藏夹批量扫描仍停留在首屏 20 条的问题，新增基于 MakerWorld `design-service` 收藏接口的专用发现链路
- 收藏夹批量扫描改为直接调用 `favorites/designs/{uid}` 分页接口，并在拿到 `total` 后动态放宽扫描页数，避免被默认页数上限截断

### 2026-04-14
- 版本号升级到 `v0.2.41`
- 全站标签页图标统一移除：SPA 入口页与服务端模板页都显式声明空 favicon，并由后端把 `/favicon.ico` 返回空响应
- 模型库卡片改回自然高度，不再被同一行最高卡片拉伸，修复底部操作按钮下方的大块空白

### 2026-04-14
- 版本号升级到 `v0.2.40`
- 删除左侧导航品牌区的 `MH` 徽标，只保留文字标题与副标题
- 模型库筛选栏改为统一固定高度，搜索框、下拉框、重置按钮保持一致

### 2026-04-14
- 版本号升级到 `v0.2.39`
- 修复左侧导航顶部“隐藏”按钮在窄宽度下被挤成竖排的问题，按钮现在固定横向单行显示
- 模型库标签筛选下拉改为 `全部 / 收藏 / 已打印 / 真实标签` 的顺序
- 模型库搜索框高度与右侧筛选框统一

### 2026-04-14
- 版本号升级到 `v0.2.38`
- 左侧导航改成文字版“显示 / 隐藏”切换：导航展开时按钮固定在侧栏右上角，导航收起后显示独立“显示”按钮
- 页面顶部留白改为仅在导航收起时为悬浮按钮让位，修复模型库顶部长期空出一整块的问题
- 模型库筛选栏去掉 `搜索 / 来源 / 标签 / 排序` 文本标签，移除结果统计区，标签筛选前置 `收藏 / 已打印`，排序新增 `发布时间`
- 首页 4 个统计卡固定同排显示；系统状态和任务摘要在窄宽度下保持并排并拉齐高度
- 任务页标题区精简为“任务总览”，顶部 `运行中/排队 / 缺失 3MF / 整理任务` 三卡固定并排；“当前归档任务”改为 `运行中 / 排队中 / 最近失败` 上下堆叠展示

### 2026-04-14
- 版本号升级到 `v0.2.37`
- 模型库顶部把导航按钮并入筛选区，改成更紧凑、更粗线条的左侧汉堡按钮，不再单独悬浮在搜索区外
- 模型库移除“选择 / 全选 / 清空 / 删除”整套批量操作，卡片上的单独删除作为唯一删除入口，同时去掉卡片复选框
- 窄宽度下模型卡片的 `点赞 / 收藏 / 下载` 固定保持一行，`本地收藏 / 已打印 / 删除` 也固定保持一行，不再拆成多行
- `采集时间 / 发布时间` 纵向信息进一步压缩，减少卡片底部高度

### 2026-04-14
- 版本号升级到 `v0.2.36`
- 把导航开关按钮进一步调整为更接近 MoviePilot 截图的左上角悬浮汉堡按钮样式，不再固定在右上角
- 页面主内容顶部留白同步上移，避免悬浮按钮遮挡首页、模型库、设置页和详情页顶部内容
- 模型库去掉“应用筛选”按钮，搜索框改为失去焦点自动应用筛选，来源 / 标签 / 排序改为切换后自动应用
- 模型库筛选区在窄屏和侧栏自动隐藏时改为更紧凑的横向压缩布局，减少顶部占用高度
- 模型卡片网格改成接近原 `mw_archive_py` 的自适应 `auto-fill + minmax` 布局，卡片宽度会随窗口宽度实时调整，不再固定为死板列数

### 2026-04-14
- 版本号升级到 `v0.2.34`
- 全站布局新增右上角三横线导航开关，可手动显示 / 隐藏左侧导航栏
- 页面宽度缩小到移动端阈值后，左侧导航会自动收起，只保留右上角导航按钮；点击后以抽屉方式展开，再次点击、点击遮罩或切换页面会自动收起
- 移动端展开导航时补充遮罩与页面滚动锁定，避免内容层和侧栏同时滚动

### 2026-04-14
- 版本号升级到 `v0.2.33`
- 模型库卡片的 `点赞 / 收藏 / 下载 / 本地收藏 / 已打印 / 删除` 全部改成图标化表达，贴近原 `mw_archive_py` 首页卡片样式
- 模型卡片里的 `采集时间 / 发布时间` 改为上下排列，避免横向信息过挤
- 去掉模型库顶部单独的“选择操作”区域块，把结果统计、多选、全选、清空、删除等操作并入筛选区

### 2026-04-14
- 版本号升级到 `v0.2.32`
- 模型库列表卡片改为贴近原 `mw_archive_py` 首页的画廊样式：补齐原模型点赞数 / 收藏数 / 下载数展示，并重排作者、来源、采集时间、发布时间信息
- 新增本地库模型状态持久化：每张卡片左下角支持直接切换“本地收藏 / 已打印”，状态写入 `/app/state/model_flags.json`
- 每张模型卡片右下角新增单独删除按钮，同时保留原有多选 / 全选 / 批量删除能力

### 2026-04-14
- 版本号升级到 `v0.2.31`
- 修复批量归档子任务可能“预扫描 45 个、最终只归档部分模型”却没有失败记录的问题：批量父任务现在会持久跟踪本批应完成的模型列表，若发现某个子任务既不在队列中、也未归档、也未进入失败列表，会自动补回队列继续执行
- 批量拆分出来的模型任务现在显式标记为 `single_model`，便于后续事件流、进度统计和任务页状态保持一致
- 新增 `/app/logs/batch_queue.log`，记录批量任务的入队、跳过、补回、完成等明细，便于在 DSM 上直接核对“45 个模型为什么最后只落了多少个”

### 2026-04-14
- 版本号升级到 `v0.2.30`
- 首页去掉固定 `5s` 自动轮询，改为订阅归档完成事件后自动刷新统计与任务摘要
- 模型库接入同一事件流，单模型归档完成后会按当前筛选条件自动重载已展开的列表页，不需要手动刷新页面

### 2026-04-14
- 版本号升级到 `v0.2.29`
- 修复 `/zh/@xxx/collections/models` 收藏夹批量扫描容易卡在首屏 20 条的问题：HTML 分页补充 `offset/limit` 等变体，并把翻页选择策略改为优先挑选“新增模型最多”的分页结果，避免被站点忽略参数后返回的第一页重复数据干扰

### 2026-04-14
- 版本号升级到 `v0.2.28`
- 任务页“开始归档”改为分流提交流程：单模型链接直接提交并弹出成功提示；作者页 / 收藏夹链接先预扫描模型数量，再弹出“确认提交 / 取消”对话框
- 批量确认提交流程会复用这次预扫描结果，不再让确认后的后台任务重复扫描同一批链接；同时修复归档接口返回 `accepted: false` 时前端仍误弹成功提示的问题

### 2026-04-14
- 版本号升级到 `v0.2.27`
- 新增轻量 `/api/bootstrap` 启动接口，前端首屏引导从 `auth/me + config` 双请求改为一次请求；非设置页不再在启动阶段预拉完整配置，进一步缩短首页与全站首次进入耗时

### 2026-04-14
- 版本号升级到 `v0.2.26`
- 优化首页和模型库接口装配逻辑：列表页与首页统计只读取轻量模型卡片字段，不再把评论、打印配置、附件、富文本描述整包返回，修复首页首次加载明显偏慢的问题

### 2026-04-14
- 版本号升级到 `v0.2.25`
- 移除模型库页残留的瀑布流 `column-count` 覆盖，恢复与全站一致的响应式网格布局，修复模型卡片只显示第一张、其余卡片位置异常的问题

### 2026-04-14
- 版本号升级到 `v0.2.24`
- 任务页输入链接并点击“开始归档”后，提交成功会弹出“任务已提交”确认弹窗，并展示后端返回的入队说明

### 2026-04-14
- 版本号升级到 `v0.2.23`
- 修复删除模型时只删除模型目录、不清理归档根目录同前缀旧版平铺资源的问题
- 归档整理阶段改为覆盖移动资源文件，避免目标文件已存在时把新下载的图片继续遗留在 `/app/archive` 根目录
- 实例配图和分盘缩略图保留与 `meta.json` 一致的文件名并统一放入模型目录 `images/` 下，继续收敛为一个模型一个文件夹

### 2026-04-14
- 版本号升级到 `v0.2.22`
- 修复归档队列可能长时间卡在“正在整理摘要与设计图片”的问题：资源下载增加开始日志、总时长上限与失败跳过，避免单个图片请求把整队卡死
- 40%-55% 阶段拆分为摘要图、设计图、评论资源三个子阶段，任务页会持续刷新具体进度，不再长时间停在单一文案
- 应用启动时会把上次异常中断留下的 `active` 任务自动回退到 `queued` 并恢复执行，部署新镜像后会自动续跑
- 补上离线归档页的回退 HTML，避免旧的 `model.html` 资源缺失时持续在日志里报错

### 2026-04-14
- 版本号升级到 `v0.2.21`
- 模型库分页默认值从 24 调整为 8，首屏先加载 8 个模型卡片
- 滚动到底部后继续按 8 个一页自动续载，保持瀑布流加载节奏更轻

### 2026-04-14
- 版本号升级到 `v0.2.20`
- 模型库接口新增分页参数，前端改为首屏加载 24 个，滚动到底部会自动续载下一页
- 模型库卡片布局改为瀑布流，适配桌面 4 列、平板 2 列、手机 1 列

### 2026-04-14
- 版本号升级到 `v0.2.19`
- 首页展示台改为自动轮询刷新，默认每 5 秒自动更新统计卡和任务摘要
- 页面切到后台标签页时会暂停轮询，回到首页标签页后立即补刷，避免必须手动刷新网页

### 2026-04-14
- 版本号升级到 `v0.2.18`
- 修复作者页批量归档在 `user/uid` 接口失效时无法继续翻页的问题
- 新增“从作者页已发现模型反推作者 UID”的兜底逻辑：会抽样读取模型详情里的 `designCreator.uid`，再回切到 `/published/{uid}/design` 补全作者全部模型

### 2026-04-14
- 版本号升级到 `v0.2.17`
- 批量发现新增 `batch_discovery.log` 调试日志，作者页扫描会记录每次 API 尝试的 URL、参数、状态码以及 `hits/total/hasNext`
- 便于直接从 DSM 的 `/volume4/docker/docker/makerhub/logs/batch_discovery.log` 复盘“为什么还是 20 个”

### 2026-04-14
- 版本号升级到 `v0.2.16`
- 修复作者页批量归档在 DSM 环境里长时间卡在“正在扫描模型链接”的问题，缩短批量发现的单请求超时并移除慢的跨域 fallback
- 作者页批量发现改为先用更大的 `limit` 拉首屏，并在后续页自动尝试 `offset / page / pageNum / pageNo / current` 参数变体，修复只拿到首批 20 条的情况
- 批量归档新增 stdout 摘要日志，后续可直接从 DSM 容器日志看到 `expected_total / discovered / queued`

### 2026-04-14
- 版本号升级到 `v0.2.15`
- 修复“输入链接开始归档”在作者页 / 收藏夹批量归档时长时间卡在“提交中”的问题
- 批量归档改为后台队列异步扫描：接口会立即返回，真实扫描与拆分模型链接在 worker 中执行，前端通过任务页查看进度

### 2026-04-14
- 版本号升级到 `v0.2.14`
- 修复缺失 3MF 单条“重新下载”后页面状态不刷新的问题，重试入队后会立即把对应模型条目标记为 `queued`
- 缺失 3MF 任务新增单条“取消”按钮，取消后会从任务列表和状态文件中移除对应条目

### 2026-04-14
- 版本号升级到 `v0.2.13`
- 修复作者上传页批量归档只扫描首屏 20 个模型的问题，改为调用 MakerWorld 实际使用的 `design-service` 分页接口
- 作者页批量发现新增 `offset + limit + total + hasNext` 联合收尾逻辑，避免继续依赖脆弱的 `?page=` 猜测分页

### 2026-04-14
- 版本号升级到 `v0.2.12`
- 修复模型详情页在深色模式下仍使用浅色背景的问题，同步覆盖详情卡片、文档区、按钮与灯箱预览的深色样式

### 2026-04-14
- 版本号升级到 `v0.2.11`
- 首页删除“最近模型”展示板块，首页只保留统计卡、系统状态与任务摘要

### 2026-04-13
- 版本号升级到 `v0.2.10`
- Docker 镜像补装 `curl`，修复容器内爬虫兜底链路缺少 `curl` 导致的归档失败
- 任务摘要新增最近失败补偿清理：已成功归档的旧失败项，以及旧镜像遗留的 `curl` 环境失败项会自动从摘要中移除

### 2026-04-13
- 版本号升级到 `v0.2.9`
- 修复 SPA 页面壳与非指纹静态资源缓存策略，避免部署后浏览器继续命中旧前端
- 去掉前端侧边栏的硬编码默认版本号，未读到配置前不再误显示旧版本

### 2026-04-13
- 版本号升级到 `v0.2.8`
- 提交后端配置接口与 SPA 路由更新，修复 `/api/config` 缺少 `app_version` 的问题

### 2026-04-13
- 版本号升级到 `v0.2.7`
- 补充提交 `frontend/src` 与 Vue 相关页面文件，修复 Docker 构建缺失源文件的问题

### 2026-04-13
- 版本号升级到 `v0.2.6`
- 修复前端 `index.html` 使用绝对路径导致 Docker 构建找不到 `/src/main.js`

### 2026-04-13
- 版本号升级到 `v0.2.5`
- 补充提交前端 `index.html`，修复 Docker 构建找不到入口文件的问题

### 2026-04-13
- 版本号升级到 `v0.2.4`
- 修复 Docker 构建里前端 `index.html` 未被复制导致的构建失败

### 2026-04-13
- 版本号升级到 `v0.2.3`
- 修复 Docker 构建缺少 `frontend/package-lock.json` 导致的失败

### 2026-04-13
- 版本号升级到 `v0.2.2`
- Docker 镜像构建新增复制根目录 `VERSION` 文件，保证线上版本号与配置一致

### 2026-04-13
- 版本号升级到 `v0.2.1`
- 缺失 3MF 重新下载成功后自动清理“最近失败”记录

### 2026-04-13
- 版本号升级到 `v0.2.0`，并改为由根目录 `VERSION` 文件统一维护
- 前端整体重构为 Vue 3 SPA，登录、首页、模型库、详情、设置、任务全部改为前端路由 + API 驱动
- FastAPI 不再渲染 Jinja 页面，只保留 `/api/*`、`/archive/*`、前端静态资源分发与登录重定向守卫
- 新增模型详情 JSON 接口 `/api/models/{model_dir}`，模型库补上多选 / 全选 / 删除选中能力
- Docker 改为 Node + Python 多阶段构建，镜像内直接携带打包后的 Vue 前端

### 2026-04-12
- 设置页新增全站主题控制，支持浅色 / 深色 / 自动三档，并可跟随系统暗色模式
- 左侧导航底部新增版本号展示，页面可直接看到当前站点版本
- 左下角改为用户头像入口，退出登录与主题切换等用户操作统一收纳进弹窗
- 模型库页删除顶部说明卡与卡片底部统计条，列表页改为更紧凑的纯筛选 + 卡片布局
- 新增登录页，公网访问默认先进入单用户登录流程
- 增加会话鉴权与 API Token 鉴权，页面、API 与 `/archive` 资源统一受保护
- 设置页新增密码修改与 Token 生成 / 撤销管理
- 归档入口接入作者上传页 / 收藏夹模型页批量扫描，并将模型链接拆分入现有队列
- 缺失 3MF 任务新增“重新下载 / 全部重试”按钮，并改成支持强制重新入队
- Docker 镜像补充 `python-multipart` 依赖，支持登录表单提交

### 2026-04-11
- 修复评论作者字段异常，避免评论区显示字典对象串
- 评论图片改为固定缩略图尺寸，并支持单击放大预览
- 模型卡片补充“采集时间 / 发布时间”双时间展示
- 设置页的国内 / 国际 Cookie 默认掩码显示，并增加显示 / 隐藏按钮
- 补充本地图片失败时的远端回退加载逻辑

### 2026-04-11
- 重构为 `首页 / 模型库 / 设置 / 任务` 四页结构
- 首页改为真实数据展示台，直接读取归档模型、配置和任务状态
- 模型库改为独立卡片页，详情页入口统一为 `/models/{model_dir}`
- 设置页改为单页内标签，合并国内 Cookie、国际 Cookie、HTTP 代理到“连接设置”
- 任务页统一展示归档队列、缺失 3MF、本地整理任务

### 2026-04-10
- 统一运行目录语义为 `/app/config`、`/app/logs`、`/app/state`、`/app/archive`、`/app/local`
- 新增根目录 `compose.yaml`，统一 DSM / Docker 部署入口
- 所有部署示例统一使用 `9042:8000`

### 2026-04-10
- 初始化 `makerhub` 项目骨架
- 建立 FastAPI、基础模板、Dockerfile、GHCR 工作流
- 接入旧项目爬虫桥接层，作为后续归档能力复用入口

设计目标：
- 复用旧项目里已经验证过的爬虫与下载逻辑
- 重写后台结构、配置模型、任务系统和前端页面
- 支持国内 / 国际 Cookie、HTTP 代理、模型归档、通知、缺失 3MF 重下、本地 3MF 整理
- 详情页按目标站做高保真复刻

当前阶段：
- 已完成 Vue 单页前端、FastAPI API 化和 Docker 多阶段打包
- 已完成真实归档数据扫描与首页 / 模型库 / 设置 / 任务 / 详情页 API 驱动展示
- 已完成模型库多选 / 全选 / 删除，以及设置页主题、Cookie、Token、密码管理
- 下一步继续围绕批量归档体验、详情页高保真复刻与本地整理任务增强迭代

运行目录约定：
- `/app/config`：运行配置
- `/app/logs`：应用日志
- `/app/state`：任务状态、JSON/SQLite 等轻量持久化数据
- `/app/archive`：归档模型目录
- `/app/local`：本地模型监测/整理目录

本地启动：

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
npm --prefix frontend install
npm --prefix frontend run build
uvicorn app.main:app --reload
```

默认情况下，本地开发环境会把运行数据写到仓库下的 `runtime/` 目录；FastAPI 会直接分发 `frontend/dist`。Docker 容器内则固定使用 `/app/config`、`/app/logs`、`/app/state`、`/app/archive`、`/app/local`。

Docker：

```bash
docker build -t makerhub .
docker run -d \
  --name makerhub \
  -p 9042:8000 \
  -v /volume4/docker/docker/makerhub/config:/app/config \
  -v /volume4/docker/docker/makerhub/logs:/app/logs \
  -v /volume4/docker/docker/makerhub/state:/app/state \
  -v /volume2/entertainment/3D打印/makerhub:/app/archive \
  -v /volume2/entertainment/3D打印/makerhub/local:/app/local \
  makerhub
```

如果希望启用设置页里的“一键更新”按钮，可额外加上：

```bash
-v /var/run/docker.sock:/var/run/docker.sock
```

Compose：

当前仓库根目录的 `compose.yaml` 已默认启用设置页网页更新能力，容器会额外挂载 Docker socket，便于直接在“设置 -> 系统”里拉取新镜像并重建自己。

```bash
version: "3.8"

services:
  makerhub:
    image: ghcr.io/s450586793/makerhub:latest
    pull_policy: always
    container_name: makerhub
    ports:
      - "9042:8000"
    volumes:
      - /volume4/docker/docker/makerhub/config:/app/config
      - /volume4/docker/docker/makerhub/logs:/app/logs
      - /volume4/docker/docker/makerhub/state:/app/state
      - /volume2/entertainment/3D打印/makerhub:/app/archive
      - /volume2/entertainment/3D打印/makerhub/local:/app/local
      # 允许设置页直接触发 Docker 网页更新
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
```

启动：

```bash
docker compose -f compose.yaml up -d
```

如果没有挂载 `docker.sock`，设置页仍会显示版本信息，但“一键更新”按钮会保持不可用，并提示你继续在宿主机执行：

```bash
docker compose pull makerhub && docker compose up -d makerhub
```
