name: Apply MakerHub 0.5.94 Patch

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/apply-makerhub-0-5-94.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply patch and remove temporary workflow
        run: |
          cat > /tmp/makerhub-0.5.94.patch <<'PATCH'
          diff --git a/README.md b/README.md
          index ba65119..d7172e2 100644
          --- a/README.md
          +++ b/README.md
          @@ -37,6 +37,14 @@ makerhub 是一个面向个人 NAS / DSM 部署的 MakerWorld 模型归档与管
           
           ## 更新记录
           
          +### 2026-04-25
          +- 版本号升级到 `v0.5.94`
          +- 缺失 `3MF` 发起重试后会立即清除首页源端状态卡上的旧验证/上限失败覆盖；如果重试过程中再次遇到验证或每日下载上限，再把首页状态更新为对应失败
          +
          +### 2026-04-24
          +- 版本号升级到 `v0.5.93`
          +- 首页源端状态卡和任务页缺失 `3MF` 列表在读取旧状态文件时，也会把历史验证失败文案统一转换为“MakerWorld 需要验证，前往官网任意下载一个模型”
          +
           ### 2026-04-24
           - 版本号升级到 `v0.5.92`
           - MakerWorld 验证提示文案统一改为“MakerWorld 需要验证，前往官网任意下载一个模型”，让首页源端状态卡和缺失 `3MF` 失败原因更直接
          diff --git a/VERSION b/VERSION
          index 83cc3a0..622db42 100644
          --- a/VERSION
          +++ b/VERSION
          @@ -1 +1 @@
          -0.5.92
          +0.5.94
          diff --git a/app/services/archive_worker.py b/app/services/archive_worker.py
          index 1dc29e9..dd483c8 100644
          --- a/app/services/archive_worker.py
          +++ b/app/services/archive_worker.py
          @@ -994,7 +994,7 @@ class ArchiveTaskManager:
                           title="" if clean_model_id else title,
                           instance_id="" if clean_model_id else instance_id,
                           model_url="" if clean_model_id else clean_url,
          -                status="missing",
          +                status="download_limited",
                           message=message,
                       )
                       return {
          @@ -1002,6 +1002,15 @@ class ArchiveTaskManager:
                           "message": message,
                       }
           
          +        self.task_store.update_missing_3mf_status(
          +            model_id=clean_model_id,
          +            title="" if clean_model_id else title,
          +            instance_id="" if clean_model_id else instance_id,
          +            model_url="" if clean_model_id else clean_url,
          +            status="queued",
          +            message="等待重新下载 3MF",
          +        )
          +
                   result = self.submit(
                       clean_url,
                       force=True,
          @@ -1034,6 +1043,15 @@ class ArchiveTaskManager:
                           status="queued",
                           message="已存在于归档队列",
                       )
          +        elif message:
          +            self.task_store.update_missing_3mf_status(
          +                model_id=clean_model_id,
          +                title="" if clean_model_id else title,
          +                instance_id="" if clean_model_id else instance_id,
          +                model_url="" if clean_model_id else clean_url,
          +                status="missing",
          +                message=message,
          +            )
                   return result
           
               def cancel_missing_3mf(self, model_id: str = "", model_url: str = "", title: str = "", instance_id: str = "") -> dict:
          @@ -1096,6 +1114,11 @@ class ArchiveTaskManager:
                   queued = 0
                   failed = 0
                   last_message = ""
          +        self.task_store.mark_missing_3mf_retrying(
          +            items,
          +            status="queued",
          +            message="等待重新下载 3MF",
          +        )
           
                   for item in items:
                       result = self.retry_missing_3mf(
          diff --git a/app/services/source_health.py b/app/services/source_health.py
          index 683426d..1a5e229 100644
          --- a/app/services/source_health.py
          +++ b/app/services/source_health.py
          @@ -495,6 +495,8 @@ def build_source_health_cards(config: Any, missing_3mf_items: list[dict[str, Any
                   ):
                       state = override_state
                       detail = str(override.get("message") or "").strip()
          +        if state in {"verification_required", "cloudflare"}:
          +            detail = describe_three_mf_failure(state, detail, url=PLATFORM_ORIGINS.get(platform, ""))
                   if not detail and state == "verification_required":
                       detail = "MakerWorld 需要验证，前往官网任意下载一个模型。"
                   return {
          diff --git a/app/services/task_state.py b/app/services/task_state.py
          index 1a0a398..c58cc1f 100644
          --- a/app/services/task_state.py
          +++ b/app/services/task_state.py
          @@ -6,7 +6,7 @@ from typing import Any, Optional
           
           from app.core.settings import LOGS_DIR, STATE_DIR, ensure_app_dirs
           from app.core.timezone import now_iso as china_now_iso, parse_datetime
          -from app.services.three_mf import normalize_three_mf_failure_state
          +from app.services.three_mf import describe_three_mf_failure, normalize_three_mf_failure_state
           
           
           ARCHIVE_QUEUE_PATH = STATE_DIR / "archive_queue.json"
          @@ -160,6 +160,8 @@ def _normalize_missing_3mf(payload: Any, fallback_items: Optional[list[dict]] =
                       message,
                       url=item.get("model_url") or item.get("url") or "",
                   )
          +        if status in {"verification_required", "cloudflare"}:
          +            message = describe_three_mf_failure(status, message, url=item.get("model_url") or item.get("url") or "")
                   normalized.append(
                       {
                           "model_id": str(item.get("model_id") or item.get("id") or ""),
          @@ -1132,6 +1134,39 @@ class TaskStateStore:
           
                   return self._update_missing_3mf(_mutate)
           
          +    def mark_missing_3mf_retrying(
          +        self,
          +        items: list[dict],
          +        *,
          +        status: str = "queued",
          +        message: str = "等待重新下载 3MF",
          +    ) -> dict:
          +        targets = _normalize_missing_3mf({"items": items or []}).get("items", [])
          +        if not targets:
          +            return self.load_missing_3mf()
          +
          +        target_keys = {_missing_3mf_key(item) for item in targets}
          +        target_urls = {
          +            str(item.get("model_url") or "").strip()
          +            for item in targets
          +            if str(item.get("model_url") or "").strip()
          +        }
          +
          +        def _mutate(payload: dict) -> dict:
          +            existing = _normalize_missing_3mf(payload).get("items", [])
          +            now = china_now_iso()
          +            for item in existing:
          +                key = _missing_3mf_key(item)
          +                item_url = str(item.get("model_url") or "").strip()
          +                if key not in target_keys and item_url not in target_urls:
          +                    continue
          +                item["status"] = status
          +                item["message"] = message
          +                item["updated_at"] = now
          +            return {"items": existing}
          +
          +        return self._update_missing_3mf(_mutate)
          +
               def upsert_subscription_state(self, item: dict) -> dict:
                   normalized_item = _normalize_subscription_state({"items": [item]}).get("items", [])
                   if not normalized_item:
          diff --git a/app/services/three_mf.py b/app/services/three_mf.py
          index 546f4ba..9b4f013 100644
          --- a/app/services/three_mf.py
          +++ b/app/services/three_mf.py
          @@ -25,6 +25,12 @@ _THREE_MF_FAILURE_PRIORITY = {
               "missing": 10,
               "available": 0,
           }
          +_THREE_MF_TRANSIENT_STATES = {
          +    "queued",
          +    "running",
          +    "retrying",
          +    "pending_download",
          +}
           _THREE_MF_FAILURE_INFERENCE_KEYWORDS = {
               "download_limited": (
                   "每日下载上限",
          @@ -176,6 +182,8 @@ def describe_three_mf_failure(
           
               if normalized_state == "download_limited" and normalized_limit:
                   return normalized_limit
          +    if normalized_state in {"verification_required", "cloudflare"}:
          +        return _default_three_mf_failure_message(normalized_state, normalized_source)
           
               legacy_messages = _THREE_MF_LEGACY_MESSAGES.get(normalized_state, set())
               if normalized_message and normalized_message not in legacy_messages:
          @@ -197,6 +205,8 @@ def normalize_three_mf_failure_state(
               url: Any = "",
           ) -> str:
               normalized_state = str(state or "").strip()
          +    if normalized_state in _THREE_MF_TRANSIENT_STATES:
          +        return normalized_state
               if normalized_state in _THREE_MF_FAILURE_PRIORITY and normalized_state not in {"missing", "available"}:
                   return normalized_state
           
          diff --git a/frontend/package-lock.json b/frontend/package-lock.json
          index 19e1735..41627ce 100644
          --- a/frontend/package-lock.json
          +++ b/frontend/package-lock.json
          @@ -1,12 +1,12 @@
           {
             "name": "makerhub-frontend",
          -  "version": "0.5.92",
          +  "version": "0.5.94",
             "lockfileVersion": 3,
             "requires": true,
             "packages": {
               "": {
                 "name": "makerhub-frontend",
          -      "version": "0.5.92",
          +      "version": "0.5.94",
                 "dependencies": {
                   "vue": "^3.5.13",
                   "vue-router": "^4.5.1"
          diff --git a/frontend/package.json b/frontend/package.json
          index 0fa0875..44834af 100644
          --- a/frontend/package.json
          +++ b/frontend/package.json
          @@ -1,7 +1,7 @@
           {
             "name": "makerhub-frontend",
             "private": true,
          -  "version": "0.5.92",
          +  "version": "0.5.94",
             "type": "module",
             "scripts": {
               "dev": "vite",
          diff --git a/tests/test_missing_3mf.py b/tests/test_missing_3mf.py
          index 370196c..76cea54 100644
          --- a/tests/test_missing_3mf.py
          +++ b/tests/test_missing_3mf.py
          @@ -76,6 +76,25 @@ class Missing3mfTest(unittest.TestCase):
           
                   self.assertEqual(len(normalized["items"]), 1)
                   self.assertEqual(normalized["items"][0]["status"], "verification_required")
          +        self.assertEqual(normalized["items"][0]["message"], "MakerWorld 需要验证，前往官网任意下载一个模型。")
          +
          +    def test_retry_state_is_not_reclassified_by_old_verification_message(self):
          +        payload = {
          +            "items": [
          +                {
          +                    "model_id": "1759345",
          +                    "title": "0.2mm 层高, 2 层墙, 15% 填充",
          +                    "status": "queued",
          +                    "model_url": "https://makerworld.com.cn/zh/models/1759345",
          +                    "message": "MakerWorld 需要验证，前往官网任意下载一个模型。",
          +                }
          +            ]
          +        }
          +
          +        normalized = _normalize_missing_3mf(payload)
          +
          +        self.assertEqual(len(normalized["items"]), 1)
          +        self.assertEqual(normalized["items"][0]["status"], "queued")
           
           
           if __name__ == "__main__":
          diff --git a/tests/test_source_health.py b/tests/test_source_health.py
          index fc02bd5..a4023cc 100644
          --- a/tests/test_source_health.py
          +++ b/tests/test_source_health.py
          @@ -41,6 +41,7 @@ class SourceHealthCardsTest(unittest.TestCase):
                   card_map = {item["key"]: item for item in cards}
                   self.assertEqual(card_map["cn"]["state"], "verification_required")
                   self.assertEqual(card_map["cn"]["status"], "需要验证")
          +        self.assertEqual(card_map["cn"]["detail"], "MakerWorld 需要验证，前往官网任意下载一个模型。")
                   self.assertEqual(card_map["global"]["state"], "ok")
           
               def test_missing_3mf_limit_overrides_probe_verification(self):
          @@ -105,6 +106,37 @@ class SourceHealthCardsTest(unittest.TestCase):
                   self.assertEqual(card_map["cn"]["state"], "verification_required")
                   self.assertEqual(card_map["cn"]["status"], "需要验证")
           
          +    def test_retrying_missing_3mf_does_not_override_probe_ok(self):
          +        original_probe = source_health._probe_platform_status
          +        source_health._probe_platform_status = lambda platform, *_args, **_kwargs: {
          +            "platform": platform,
          +            "state": "ok",
          +            "status": "连接正常",
          +            "detail": "",
          +        }
          +
          +        class Config:
          +            cookies = []
          +            proxy = None
          +
          +        try:
          +            cards = source_health.build_source_health_cards(
          +                Config(),
          +                [
          +                    {
          +                        "status": "queued",
          +                        "message": "MakerWorld 需要验证，前往官网任意下载一个模型。",
          +                        "model_url": "https://makerworld.com.cn/zh/models/789",
          +                    }
          +                ],
          +            )
          +        finally:
          +            source_health._probe_platform_status = original_probe
          +
          +        card_map = {item["key"]: item for item in cards}
          +        self.assertEqual(card_map["cn"]["state"], "ok")
          +        self.assertEqual(card_map["cn"]["status"], "连接正常")
          +
           class InlineExecutor:
               def __init__(self, *args, **kwargs):
                   pass
          PATCH
          sed -i 's/^          //' /tmp/makerhub-0.5.94.patch
          git apply --index --whitespace=nowarn /tmp/makerhub-0.5.94.patch
          rm .github/workflows/apply-makerhub-0-5-94.yml
          git add -A
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "clear stale missing 3mf source health on retry"
          git push
